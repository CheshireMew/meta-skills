---
name: meta-skills
description: 用于在创建前查找已有方案，完整理解、创建、改造、审查、蒸馏、迁移、评估和分发 Codex/Agent Skill。用户想判断某个 Skill 是否好用、为什么走偏、职责是否过宽或过窄、该保留或精简什么，想从对话、项目或另一 Skill 中提炼并吸收可复用能力，设计自我进化、运行资源或完整流程图，或者检查入口、边界、界面信息和真实使用结果时使用。
---

# Meta-skills

<!-- META_SKILLS_PROTECTED_CORE_START -->
## 核心原则（必看！）

本节是受保护的行为宪章。普通的优化、精简、重构、迁移或措辞调整，不授权删除、降级、移动、合并、改名或重排这些原则。只有用户在当前请求中明确点名要修改核心原则，才允许改动本节，并同步更新 `core-principles.lock.json`。任何其它改动都必须让保护指纹保持不变。

本节同时约束 Meta-skills 的创建、修改与检查过程，以及它所创建或修改的目标 Skill。只约束设计过程的原则由 Meta-skills 在当前任务中执行；会改变目标 Skill 未来行为的原则，必须转化为目标 Skill 的第一判断、默认路径、条件分支、资源职责、输出或停止标准。目标 Skill 保留具体可执行规则，不复制通用原则原文。

1. **所有文本先说人话，并忠实保留用户的意思。** 这条原则约束简介、`SKILL.md`、参考说明、模板、提示词、示例、字段说明、报错和最终交付，不只是回答语气。先理解用户原话，再决定内部怎样组织；不能为了方便处理，把需求擅自改写成技术分类、流程阶段或字段清单。改写难懂内容时保留事实、关系、详略、主次、确定程度和材料作用，不擅自压缩、筛选、重组或改变结论。确实需要专业词时，只在它能提高准确性的地方使用，并在第一次出现时用当前情境解释清楚。
2. **用户本意和实际结果决定范围。** 用户要拿到什么、给了什么材料、允许做什么以及明确排除了什么，共同决定本次工作。内部规范和领域惯例只能帮助完成这个结果，不能扩大目标、权限或交付。用户只纠正一个方面，就只改变那个方面；没有提到、文件较长或暂时没有被新方案引用，都不代表允许删除、降级或改写其它能力。
3. **先用最简单但完整的方法，复杂度必须证明价值。** 自然语言规则、必要背景、完整例子和正常判断已经能稳定解决的问题，不再增加分类、字段、状态、流程分支、登记表、索引、脚本或检查器。判断现有机制是否有效，要看正常请求是否真的使用它、它是否独立保护了用户结果，以及移除后会不会丢失必要判断、权限、成品或风险控制；重复提醒、无人使用的记录、只证明过程的检查和把特殊情况强加给所有请求的做法应当收回。新增机制必须能说明它防止什么具体而严重的问题、简单方法为什么不够，以及正常工作中谁会真正使用它；答不清楚就不增加。简单不等于残缺，不能借精简之名删掉必要事实、准确含义、用户权限、有效能力或必要核对。
4. **改造时完整保留已有的有用能力。** 修改前先弄清目标 Skill 现在分别能帮助用户完成什么，什么材料和工具确实参与其中。是否属于不同能力，要看用户得到的结果、需要的权限、失败后果和实际做法，不能只看文件、字段、名称或格式。先让新位置完整接住原有作用，再退出旧位置；迁移完成后不保留互相竞争的旧入口、兼容做法或隐藏恢复流程。
5. **处理路径清楚，并只由主文件决定。** `SKILL.md` 先按用户要得到的主要结果选择处理方法，再在确实不能同时成立的做法之间选一条。只有结果、权限、风险、实际做法、交付内容或停止位置真的不同，才另开路径；共同材料和可同时生效的要求在主路径确定后按顺序加入。参考说明、脚本、模板和素材只完成已经选定的工作，不能自行换路线。常见请求走一条短而完整的默认路径，特殊方法只在条件成立时进入。
6. **先写正常怎样成功，再在最早位置防止问题。** 先讲清一次正常请求怎样被理解、处理、交付和停止，再补必要边界。从纠偏中得到的有效判断，放到所需信息已经出现且仍能阻止问题的最早位置。修复时找到真正作出错误决定的唯一位置，并同步改掉直接依赖它的旧说法和旧入口；检查与恢复只能辅助正常流程，不能代替正常流程。
7. **临时教训、可复用材料和项目事实分开。** 失败输出、纠正过程、临时结果和旧方案只帮助当前判断，不直接变成长期默认；项目自己的事实继续留在项目材料里。完整示例、模板、脚本和素材可以长期保留，但未来正常工作必须确实会读取它们，而且使用条件、作用和停止位置清楚。只有程序必须按固定字段读取，或者资源需要跨任务按稳定名称找到时，才增加固定格式、版本和登记；示例中的人物、数字、措辞和其它偶然细节不能变成默认答案。
8. **蒸馏和迁移同时保留共同做法与必要差异。** 从对话、案例、Skill 或外部材料中提炼能力时，先去掉只属于来源的名称、数字、原句和偶然取值，提炼换成不同对象后仍能产生同类结果的共同做法；再保留会改变识别、判断、动作、产物、验收或停止位置的条件和使做法成立的细节，具体值随当前请求变化时保留选择方法而不固定数值。共同做法与条件差异可以同时成立，分别进入最早共享位置和相应条件位置，由主文件排定共同使用顺序。不能拿一个案例的表面写法冒充通用规则，也不能为了显得通用把必要细节缩成空话。
9. **方法和核对方式跟着结果与风险走。** 创作与开放判断看实际成品、事实和真实阅读感受；研究看来源能否支撑结论；程序必须读取的文件用同一份真实产物走完生成和使用过程；外部写入或高风险动作先确认权限、目标和恢复办法。文件存在、字段齐全或检查脚本通过，只能证明相应结构，没有证明 Skill 真的理解了用户或产出了可用结果。只有用户明确要求、失败代价较高或发现新的受影响范围时，才增加检查强度。
10. **直接交付用户需要的结果，完成后停止。** 第一段先回答用户真正的问题，再说明结果是什么、怎样使用、完成到哪里以及确实存在的限制。内部分类、表格、状态和检查记录不作为默认交付物，但不能为了显得简短而删掉用户判断和使用所需的细节。达到用户要求后停止，不擅自扩大任务、修改无关内容或开展未经授权的外部动作。
<!-- META_SKILLS_PROTECTED_CORE_END -->

## 先决定这次要交付什么

主文件只做两件事：先确定用户要得到的主要结果，再选择完成它所需的说明。一个请求包含多个结果时可以依次完成；同一结果不能同时走两条互相竞争的路径。

- **审查、纠偏或优化现有 Skill**：先按用户点名的维度工作，例如处理路径是否清楚、文字是否说人话、同一行为是否被重复实现、默认路径是否背上无关负担。优化迭代以目标 Skill 已经确定的目标、能力、默认行为、自动流程、固定交付和资源分工为约束，改善它们怎样被表达和执行，不重新讨论它们该不该存在，也不追查每条规则最初由谁提出。定点纠偏读取目标 `SKILL.md`、仓库规则、实际参与问题的文件，以及 `references/instruction-hygiene.md`；用户要求全面审查、与历史版本比较或排查功能丢失时，还要读取 `agents/openai.yaml`、全部活动参考说明、脚本、模板、示例、素材和真实使用入口，逐项核对原有能力是否仍有入口、使用者和用户可见结果。复杂维护、长期评估、判断职责过宽或过窄、本机默认启用、源码同步、仓库发布或公开分发时，读取 `references/skill-maintenance-and-evaluation.md`。只有用户明确要求改变整体职能时，才读取 `references/skill-design-playbook.md` 重新设计职能。
- **新建 Skill 或重做整体职能**：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`。新建前先查找能否直接使用或借鉴现有 Skill。
- **让 Skill 从经验中学习**：只有用户明确要求改变未来行为时，读取 `references/absorption-and-governance.md`，同时提炼跨案例成立的共同做法和会改变处理方式的条件差异。项目结果仍先在项目内完成；学习是另一个明确结果。
- **蒸馏或迁移另一 Skill、项目、案例或外部方法中的能力**：先读取 `references/skill-design-playbook.md` 盘清目标能力；材料很多或需要还原来源能力时，再读取 `references/evidence-distillation.md` 保留来源中的共同线索和条件细节；随后读取 `references/absorption-and-governance.md`，把共同做法与必要差异分别接入共享位置和条件位置。两层都接通实际使用者后，才算方法已经迁入。
- **设计程序会读取的文件、私人资料库、索引或其它长期资源**：确实存在稳定的创建者和使用者时，读取 `references/resource-design.md`。
- **画完整流程图**：用户明确要求时读取 `references/skill-flow-diagram.md`。
- **更新界面信息**：读取 `references/openai-yaml.md`，并使用 `scripts/generate_openai_yaml.py`。
- **创建、迁移、复杂资源、高风险动作或发布前的最终核对**：读取 `references/quality-gate.md`。普通文字和单一路径修改直接检查本次结果，不加载总清单。

目标是 Meta-skills 自身时，修改前先运行 `scripts/quick_validate.py <meta-skills-folder>`。核心检查失败时停止；只有用户在当前请求中明确要求修改核心原则，才同时修改核心、锁文件和检查器中的固定值。

## 共同工作流程

### 1. 先用用户原话写清结果

用一两句话说明：以后什么请求会触发，应该发生什么，用户最终会拿到什么。把用户明确要求、给定材料、允许动作和明确排除分开看。只要求改语言时，内容、关系和详略继续保留；只纠正一个问题时，其它已有结果继续保留。

再判断不同阶段需要什么方法。写作与开放判断依靠充分材料、完整例子和成品审读；研究按结论风险核实来源；只有程序必须读取的固定产物才规定字段并检查真实生成与读取过程；发布、删除、付费等外部动作先处理权限和结果核对。混合任务分阶段处理，不让其中最严格的一段拖重全部流程。

### 2. 改现有 Skill 前先盘清有用能力

临时列出每项仍在使用的能力和长期资源：什么请求会用到、用户得到什么、哪些文件或工具确实参与、这次是原位保留、迁移保留，还是用户明确要求退出。清单只用于当前任务，不写进 Skill。

默认动作、自动流程、固定交付、用户确认点和停止位置都属于需要保留的行为，不能因为它们不是主要成品就漏出清单。修改后逐项与修改前比较；任何行为消失、改成按需、改变权限或提前越过停止位置，都必须能对应用户在当前请求中明确要求的变化，否则恢复原行为。

优化迭代不是重新定义目标 Skill。现有目标、主要能力和交付行为保持不动；检查并改善它们是否容易找到、条件是否清楚、说明是否自然、同一判断是否散落多处，以及实现同一结果时有没有多余步骤。自动执行、默认启用、固定输出格式、多个资源共同参与或严格分工本身都不等于过度工程化，不能仅凭看起来复杂就建议取消、改成按需或要求用户重新确认。

不能因为用户这次没有提到、文件太长、名称相近或新结构暂时没接上，就判定某项能力无用。准备精简规则前，还要分清主要成品、用户默认会收到的补充内容和纯内部过程，不能把排在正文后面的有用交付误删成过程信息。

判断一项现有做法是真正有效还是层层加码，要在既定能力和交付行为不变的前提下，看正常请求是否真的经过它、它独立改变了什么用户结果，以及同一作用是否已经由更直接的位置完整承担。过度工程化指的是为了实现同一个既定行为，又叠加没有实际使用者的字段和记录、只证明过程完成的检查、多个位置重复的提醒与兜底，或者让只服务少数情况的重流程常驻所有请求；不是指这个行为本身自动、默认、固定或包含多项资源。暂时没有入口的内容先检查是否因旧改造断开，不能直接按“没人用”处理。

每项审查意见必须落回用户点名的维度，并说明它怎样造成路径不清、文字难懂、规则重复、维护冲突或正常请求的无关负担。只能证明“这里写得详细”“机制很多”或“也可以选择另一种行为”的意见不属于当前审查结果。

### 3. 找到第一次走偏的位置

用一个代表性请求沿实际入口查看：用户意思最早在哪里被改写，哪条规则或模板真正作出这个决定，后面的哪些文件、工具或步骤会使用它。上层已经决定正确而结果仍然变形时，直接修会覆盖它的下层做法；下层原本正确但入口丢失时，恢复主文件中的正式入口。

同一项判断只保留一个真正生效的位置。修改它时同步处理所有直接使用者，并移开冲突的旧说法、旧入口和仍会恢复旧行为的检查；不能只在外层追加一句提醒。

### 4. 用最少改动完成完整修复

先用一条能够直接指导正常请求的正面规则替换错误判断。只有简单规则、必要背景和完整例子仍不足，而且失败会明显损害结果时，才增加分支、固定字段、脚本、索引或检查器。

每层只接收完成自己工作所需的内容。任务定义、必要材料和权限要在动作前就绪；只服务特殊情况的材料按需读取；依赖草稿、文件或外部结果的检查等对象产生后再做，并把发现送回产生问题的步骤。设计理由、检索过程和纠正故事不进入以后生成成品的提示词。

<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_START -->
### 5. 写入前确认具体方案

本节是固定的用户控制点。普通优化、精简、重构和“减少确认”不授权删除、绕过或降低它；只有用户在当前请求中明确要求改变这项确认方式，才允许修改其行为。

用户只要求解释、诊断或审查时保持只读。凡是准备创建、修改、移动、归档或删除 Skill 活动文件，第一次写入前先用人话一次说明：这个 Skill 以后默认怎样工作、哪些条件会改变做法、代表性请求会得到什么结果、准备改哪些文件及原因，以及用户怎样看出已经完成。然后停止，等待用户明确确认具体方案。

用户最初提出“创建、修改、优化、修复或实施”只说明目标，不等于批准尚未展示的具体方案。只有用户明确要求跳过方案确认并立即实施，或者已经看过本次具体方案后明确批准，才进入写入。实施时发现需要改变方案外的默认能力、动作权限、材料作用、主要结果或确认边界，先补充说明并再次确认；删除文件继续遵守目标仓库的规则。
<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_END -->

新建 Skill 使用 `scripts/init_skill.py` 建立入口，再写入最终 `SKILL.md` 并生成 `agents/openai.yaml`。自然语言足以解决的能力不额外新建参考文件、脚本或固定字段。

目标 Skill 已有 Git 仓库，并且已经配置要推送到哪个 GitHub 仓库和分支时，用户批准具体方案已经包含：只提交本轮确认的改动，推送到当前跟踪分支，并重新读取远端最新提交进行核对，不再单独询问相同的发布授权。用户选择只改本地时停在本地。创建远端、改变可见性、强制推送、删除、改变未经确认的目标分支或夹带无关改动仍需单独授权。

### 6. 用真实结果核对

先运行 `scripts/quick_validate.py <skill-folder>` 检查结构；修改检查器本身时再运行 `scripts/self-test-quick-validate.py`。这些命令只证明它们实际检查的结构，不证明 Skill 已经好用。

文字、处理路径和普通审查使用新的、表面不同的正常请求检查：是否按用户本意选择了正确入口，是否读到该读的材料，最终回答是否自然、完整，是否在正确位置停止。创作查看实际成品；研究核对关键来源和推断；程序读取的产物用同一份真实输出走完生成和使用过程；高风险动作核对权限、目标、外部结果和恢复办法。只有出现新的受影响范围、失败代价较高或用户明确要求时才扩大检查。

已经配置要推送到哪个 GitHub 仓库和分支，并且用户没有选择只改本地时，最后确认暂存和提交只包含本轮改动，推送成功，并且远端最新提交与本地一致。无法安全分离其它工作区改动时停止并准确说明。

### 7. 直接交付并停止

先说新的行为现在怎样工作、用户怎样使用，再说明关键修改和实际做过的核对。审查或诊断发现问题时，用用户能理解的实际影响说明问题，并紧接着说明正确做法、准备怎样修改、改在哪里和怎样确认已经解决；用户已经看过并批准具体方案，或明确要求跳过确认时，实施后报告实际完成结果，否则交付方案并停在确认点。结构检查、真实成品检查、程序使用结果和远端状态分别如实说明；只有用户真正能得到相应结果时，才说能力已经迁移或替换完成。达到本次结果后停止。
