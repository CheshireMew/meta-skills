---
name: meta-skills
description: 用于判断一项能力应由模型或 Agent、确定性程序、软件或服务还是组合完成，并选择提示词、模板、Skill、CLI、API、GUI 或插件等合适包装与入口；也用于在创建前查找已有方案，完整理解、创建、改造、审查、蒸馏、迁移、评估和分发 Codex/Agent Skill。用户想选择能力载体，判断某个 Skill 是否好用、为什么走偏、职责是否过宽或过窄、该保留或精简什么，想从对话、项目或另一 Skill 中提炼并吸收可复用能力，设计自我进化、运行资源或完整流程图，或者检查入口、边界、界面信息和真实使用结果时使用。
---

# Meta-skills

<!-- META_SKILLS_PROTECTED_CORE_START -->
## 核心原则

本节保护 Meta-skills 的稳定用户控制权。它同时约束 Meta-skills 自身和它修改的目标 Skill。

1. **用户结果定义工作。** 先还原用户要得到的结果、提供的材料、明确决定和权限，范围、方法、产物与验收都从这些信息出发。
2. **明确决定保持稳定。** 用户确认的目标、材料边界、系统选择、资源职责和输出约定继续成立。实现可以简化或替换；改变明确决定时先取得用户确认。
3. **一次修改完成一次替换。** 找到产生问题的活动规则、材料交接、资源或程序，重写受影响部分并同步直接使用点。新实现完整承接用户结果后，旧实现退出。
4. **判断交给正确的执行者。** 同一输入下的正常理解、取舍和创作由模型完成。会改变用户结果的路由、阶段、资源、权限和高影响动作由用户选择或授权。
5. **工作输入保持干净。** 每份信息只进入真正使用它的环节。交给下游 AI 的输入由用户目标、原始材料、必要参考、固定协议和输出要求组成，维护记录留在维护侧。
6. **证据服务最终结果。** 客观结果使用真实产物、状态和来源核对，主观结果交给用户判断。测试与检查器说明各自直接覆盖的技术约定。
7. **按真实状态交付。** 达到目标时交付可用结果，验收失败时交付准确状态和下一项用户决定。说明围绕用户理解和使用结果所需的信息展开。
<!-- META_SKILLS_PROTECTED_CORE_END -->

## 按本次工作读取说明

- **审查、纠偏或优化现有 Skill**：读取目标 `SKILL.md`、仓库规则和直接影响问题的文件。用户原意已经被改写、输入需要跨阶段交接，或者涉及开放创作和参考材料时，再读 `references/instruction-hygiene.md`；涉及历史反复、旧版本比较、复杂维护、默认启用或发布时，再读 `references/skill-maintenance-and-evaluation.md`。只有用户要求重新确定整体职能时，才读 `references/skill-design-playbook.md`。
- **选择能力载体、新建 Skill 或重做整体职能**：读取 `references/skill-design-playbook.md`。需要设计跨阶段输入交接，或者涉及开放创作和参考材料时，再读 `references/instruction-hygiene.md`；涉及确定性程序、Agent 宿主、外部软件或服务、CLI/API/GUI/插件入口、本机配置或长期资源时，再读 `references/resource-design.md`。创建前先按实际执行者、包装和入口查找已有方案。
- **让 Skill 从经验中学习或设计自我维护**：只有用户明确要求改变未来行为时，读取 `references/absorption-and-governance.md`，先区分目标 Skill 职能内的领域资源维护与由 Meta-skills 负责的架构变化；涉及程序读取或长期更新的资源时，再读 `references/resource-design.md`。
- **蒸馏或迁移外部能力**：读取 `references/skill-design-playbook.md` 和 `references/absorption-and-governance.md`；材料很多或需要还原来源做法时，再读 `references/evidence-distillation.md`。
- **设计程序会读取的文件、私人资料库、索引、可复现实验或其它长期资源**：确实存在稳定的创建者和使用者，或者需要锁定输入、版本和运行记录时，读取 `references/resource-design.md`。
- **画完整流程图**：用户明确要求时读取 `references/skill-flow-diagram.md`。
- **更新界面信息**：读取 `references/openai-yaml.md`，并使用 `scripts/generate_openai_yaml.py`。
- **创建、迁移、复杂资源、高风险动作、顽固问题的独立验收或发布前的最终核对**：读取 `references/quality-gate.md`。普通文字和单一路径修改直接检查本次结果，不加载总清单。

目标是 Meta-skills 自身时，修改前先运行 `scripts/quick_validate.py <meta-skills-folder>`。它同时检查活动文本文件的外层工具预算；核心检查失败时停止。只有用户在当前请求中明确要求修改核心原则，才同时修改核心、锁文件和检查器中的固定值。

## 共同工作流程

### 1. 只读还原问题与修复依据

完整读取用户指定的会话、项目、真实结果和相关活动文件。用临时清单记录用户结果、必要材料、工具、权限和已经批准的决定，并把现有实现分别映射为保留、替换或退出。已经由结果和链路支持的原因、用户确认的未来结果与未知部分分别记录。

用户要求完整会话、项目过程或历史时，使用当前宿主的原生入口读取到最早记录，按“要求—结果—反馈—修改—下一次结果”还原变化。单次成品问题直接处理成品；稳定规则只承接用户明确要求的未来结果或已经证明的长期原因。

读取当前工作区状态与差异，保护能够辨认的已有工作。目标会写文件时，查清正式入口、默认参数、下游使用者和所有实际写入位置，区分 Skill 源码、用户指定项目、Skill 本地运行区与外部交付位置。

创建、修改或自我进化 Skill 时，枚举全部活动 UTF-8 文本文件并运行 `scripts/file_budget.py <skill-folder>`。模型会完整读取的文本与结构化素材进入 9,000 tokens 的单文件预算；归档、依赖、缓存、构建目录、运行产物和二进制媒体沿用各自的资源职责。

### 2. 形成一个可确认的替换方案

方案说明主要修复依据、未来用户结果、保持与改变的明确决定、必要输入、系统选择、活动资源、文件范围、退出的旧实现和真实核对方式。目标产生文件时，同时说明生产根、外部交付位置和既有错放产物的处理。简单方法能够完整承接结果时直接使用；确认后一次迁移所有直接使用点。

目标 Skill 能修改自身时，把文件预算纳入它自己的写入前后检查。架构、路由、资源系统或检查器发生变化前，让一项代表性正常请求沿正式入口走到真实使用者，用失败位置说明变化的必要性。抽象名称对应到运行时真正加载的模板、默认值和参考。速度结论测量从确认输入到第一份可用结果的完整链路。

通用方法先与领域规则分开，再用两种结果机制明显不同的 Skill 检查适用性。共同方法进入 Meta-skills，领域事实、专业边界和关键资源留在对应 Skill。

开放创作型 Skill 的质量依赖用户本人的情绪、审美、观点或取舍时，可以提供 Plan 模式访谈。默认模式直接完成任务；用户开启 Plan 模式后，先读取现有材料并完成当前权限内的调查，再使用 `request_user_input` 每轮询问一个只有用户能够回答的主观问题。用户有辨识度的原话、矛盾和边界进入后续创作材料，已有的付费、发布、外部写入和分阶段确认继续生效。

<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_START -->
### 3. 写入前确认具体方案

准备改变 Skill 活动文件时，先向用户提交一份具体方案，说明未来用户结果、继续保持的决定、真正改变的决定、退出的旧实现、涉及的文件与资源，以及真实核对方式。系统路由、处理阶段、动作权限和高影响操作的变化在方案中明确列出。

用户确认方案后进入写入。实施保持修复依据、用户结果、必要输入、系统选择、活动资源、文件范围和验收方法与获批方案一致；其中任一项发生变化时，先提交更新后的方案。解释、诊断和审查保持只读，用户明确要求立即实施时按其授权执行。
<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_END -->

### 4. 一次实施并清除旧实现

按获批方案重写真正产生问题的位置，并同步模板、脚本、测试、界面和其它直接使用点。确定性实现错误在同一方案内修正；方案合同发生变化时返回确认阶段。

超出文件预算的内容按唯一职责迁移完整章节、内聚函数族或测试主题，同时保持入口、读取顺序、公共调用、测试身份和内容精度。开放创作型 Skill 先读取最终进入创作模型的运行提示词，让测试覆盖输入、资源、路由和硬边界。

Skill 生成的制作真源、运行项目、中间文件、预览、报告和成品进入由代码位置确定的 Skill 本地忽略运行区。用户指定外部交付时，在真实结果通过后复制点名成品；用户要求修改现有项目时，该项目成为本次写入目标。

### 5. 核对真实结果并停止

先运行 `scripts/quick_validate.py <skill-folder>`；修改检查器时再运行 `scripts/self-test-quick-validate.py`。随后执行与结果成立方式匹配的真实核对：研究检查来源，程序让同一份产物经过正式生成和读取，外部动作核对目标与状态，开放结果展示完整候选供用户判断。生成文件的 Skill 还要从不相关仓库调用正式入口，核对本地运行区和获准的外部交付位置。

测试、阈值和检查器只说明它们直接覆盖的技术约定。最终核对重新使用最初失败的真实请求和正式活动路径。结果成立时交付；结果失败时保留准确差异、当前状态和需要用户决定的下一步。

仓库发布遵循 `references/skill-maintenance-and-evaluation.md`。目标仓库已有跟踪远端时，默认以当前整个工作区为发布单元，完成共同验证、提交和推送。用户选择只改本地时保留本地状态。
