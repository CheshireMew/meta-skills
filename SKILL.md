---
name: meta-skills
description: 用于在创建前查找现有方案，完整学习、创建、改造、审计、迁移、评估和分发 Codex/Agent Skill 及其能力的元技能。Use when the user asks to study another Skill end to end and decide what the target should absorb, design or scaffold a new skill, generate its SKILL.md and agents/openai.yaml, refactor or audit an existing skill, infer whether its role is too narrow or broad from available conversation history, create or improve a self-evolving skill that internalizes validated dialogue lessons into its own behavior, transfer a user-result-producing capability across Skills, files, resources, tools, providers, or architectures, draw a detailed workflow, control, data, resource, or verification chain diagram for a Skill, migrate scattered workflow rules into SKILL.md/references/scripts/assets, separate transient diagnostic evidence from persistent runtime examples and resources, absorb reusable mechanisms without retaining correction history, or validate skill routing, boundaries, resources, metadata, and observable behavior.
---

# Meta-skills

<!-- META_SKILLS_PROTECTED_CORE_START -->
## 核心原则（必看！）

本节是受保护的行为宪章。普通的优化、精简、重构、迁移或措辞调整，不授权删除、降级、移动、合并、改名或重排这些原则。只有用户在当前请求中明确点名要修改核心原则，才允许改动本节，并同步更新 `core-principles.lock.json`。任何其它改动都必须让保护指纹保持不变。

本节同时约束 Meta-skills 的创建、修改与验收过程，以及它所创建或修改的目标 Skill。只约束设计过程的原则由 Meta-skills 在当前任务中执行；会改变目标 Skill 未来行为的原则，必须转化为目标 Skill 的第一判断、默认路径、条件分支、资源职责、输出或停止标准。目标 Skill 保留具体可执行规则，不复制通用原则原文。

1. **先按结果性质和失败代价选择方法。** 不把所有任务都做成同一种流程。创作与判断依靠充分材料、多个好例子和模型综合判断；研究与分析按结论风险核实来源；确定性的机器产物才使用 schema、脚本和逐段链路验证；高风险或外部写入先处理权限和可逆性。混合任务逐阶段判断，不能让其中最严格的一段绑架整条流程。
2. **用户本意和可观察结果优先。** 用户明确要求的结果、材料来源和允许动作决定范围。先看用户最终读到、看到、拿到或能够使用什么，再决定内部结构；领域惯例和完整性增强只能帮助完成这个结果，不能擅自扩大目标、权限或交付。
3. **有效能力按用户结果保全，不按实现细节增殖。** 改造现有 Skill 前，先确认它现在能为用户完成哪些事情以及哪些资源确实参与其中。只有差异会改变用户结果、动作权限、失败风险或必要方法时，才保留独立分支；字段、文件、名称、格式或中间步骤不同，不自动构成不同能力。迁移完成前不提前拆掉仍有用的入口，迁移完成后也不保留旧架构和兼容层。
4. **简单是默认，复杂度必须证明自己。** 新增路由、合同、状态、schema、脚本、索引或校验器前，必须说清它防止哪一种会实质损害结果的失败，以及为什么现有上下文、例子、自然语言规则或模型判断不足以解决。说不清就不增加。精准不是堆约束；尤其在创作任务中，不把审美判断伪装成字段、打分、采用记录或流程证明。
5. **正面流程要在最早判断点解决问题。** 先写正常请求应该怎样理解、行动、交付和停止，再补必要边界。从经验或纠偏中得到的有效判断放到问题发生前最早可以控制的位置；检查和恢复只补充正常流程，不能代替正常流程。
6. **验收服从结果性质和风险。** 创作结果主要看成品、事实是否可靠、是否符合用户反馈和真实阅读感受，不要求证明模仿了哪个案例；研究结果核对来源与推断；确定性机器产物沿真实生产和消费链验证；高风险动作增加独立确认。结构检查只验证结构，不冒充结果质量。
7. **治理证据、创作参考和机器资源分开管理。** 失败输出、纠偏和旧方案只用于提炼稳定判断。创作案例与钩子可以分别存放、共同进入上下文，让模型自由综合，不强迫逐项采用或建立机器式追溯。只有确实由程序解析、跨任务寻址或稳定复用的资源，才需要标识、版本、schema、注册和冷启动验证。
8. **路由只保留真正改变做法的选择。** `SKILL.md` 负责按用户结果选择方法；只有会改变结果、权限、风险或必要手段的差异才建立分支。reference、script 和 asset 执行已经选定的职责，不反向改路由。默认路径应当短而完整，按需能力在条件成立时加入，不能为了覆盖所有可能性把普通任务变成长流程。
9. **交付要让用户直接看见结果。** 第一句先回答用户问题；最终交付说明结果是什么、用户怎样使用、是否完成。内部字段、分类、台账和检查标签只服务必要执行，不能代替成品，也不能把过程负担转交给用户。
<!-- META_SKILLS_PROTECTED_CORE_END -->

## 路由

路由只在这里决定。先看当前阶段要产出什么、失败会造成什么，再选足够的方法；reference 只能执行已选职责，不能把普通任务升级成更重的流程。

### 1. 按结果性质选方法

- **创作与开放判断**：给足当前材料、相关完整示范和少量正面要求，让模型综合判断；用成品审读验收。
- **研究与分析**：根据结论的时效、争议和风险核实来源，区分事实、推断与观点。
- **确定性机器产物**：程序读取的 schema、索引、转换结果或固定格式，才建立精确合同、生产者、边界、消费者和真实链路验证。
- **高风险或外部动作**：发布、发送、删除、付费、账户操作和不可逆写入，先确认权限、目标与可逆性，再核对外部结果。

混合任务逐阶段选方法。例如“研究后写短帖”只在研究阶段核实来源，“文案加机器可读发布包”只对发布包使用 schema。

### 2. 防止下层架空上层决定

先冻结本次改动直接涉及的用户可见默认行为、动作权限和交付结果。用户没有点名改变或退出的部分作为本轮不变量保留；精简、重构和统一措辞不能替代这项授权。

再找到目标 Skill 已经作出的上层决定，沿真实调用点看下层实际收到什么、做了什么、怎样验收，同时反向检查上层重写有没有遗漏、缩小或退出下层原本提供的有效能力。上层已经正确而结果仍然变形时，优先检查下层是否扩大了方法、输入、输出、权限或验证强度；下层原本正确而上层改写后不可达时，恢复它在上层的正式入口。

发现下层架空时，修复路由和唯一行为所有者：让下层只执行上层选中的职责，移除同义判断、冲突提示和强制旧流程的校验。不能在上层再加一句提醒，同时保留会稳定覆盖它的下层机制。reference、script、模板和校验器都服从这条规则。

### 3. 按改动性质选择主路径

- **普通优化、纠偏或审计**：读取目标 `SKILL.md`、仓库规则和直接参与该行为的活动资源，按下方短流程替换最早的错误判断。只有证据表明公共入口或其它消费者受影响时才扩大范围；不自动加载设计、吸收、资源和总检查方法。
- **新建 Skill 或重做整体职能**：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`。
- **诊断复杂的长期维护、默认启用、源码同步或用户明确要求分发**：读取 `references/skill-maintenance-and-evaluation.md`。
- **自我进化或从历史经验学习**：只在用户明确要求改变 Skill 未来行为时读取 `references/absorption-and-governance.md`。当前项目结果与 Skill 学习分开；项目事实留在项目真源。
- **能力迁移或吸收外部实现**：读取 `references/skill-design-playbook.md`、`references/absorption-and-governance.md`；需要还原外部材料时再读 `references/evidence-distillation.md`。
- **机器资源、私人库、schema、索引或资产**：确实存在独立生产和消费链时读取 `references/resource-design.md`。
- **流程与链路图**：用户明确要求时读取 `references/skill-flow-diagram.md`。
- **更新 `agents/openai.yaml`**：读取 `references/openai-yaml.md`，使用 `scripts/generate_openai_yaml.py`。
- **创建、迁移、机器资源或高风险动作的最终复核**：读取 `references/quality-gate.md`；普通文字和路由优化直接按本次结果验收。

目标是 Meta-skills 自身时，修改前运行 `scripts/quick_validate.py <meta-skills-folder>`。受保护核心失败时停止；用户没有明确点名修改核心原则时，不改核心、锁文件和对应校验常量。

## 工作流

### 1. 写清这次要改变的结果

用一两句话写清目标 Skill 以后遇到什么请求、应当怎样做、用户最终看到什么。纠偏只改变用户点名的方面；没有被点名的事实关系、主要能力、可见交付和动作权限继续保留。只要求改语言或受众，不会自动授权新增分类、筛选、压缩、叙事框架或中间格式。

再标记当前阶段属于创作、研究、确定性机器产物还是高风险动作。只有机器产物追加字段和生产消费合同；只有高风险动作追加权限与可逆性。普通优化不为了显得完整建立行为表、采用记录或过程证明。

### 2. 找到最早的错误判断和唯一所有者

沿代表性请求找到第一次偏离用户目标的位置，并确认哪个活动规则、模板、script 或消费者真正决定了它。先问三件事：上层是否已经作出正确决定；下层有没有扩大或改写它；校验器是否仍在强制旧行为。

普通优化只记录现有结果、真正入口、唯一所有者和直接消费者。只有能力迁移、公共合同变化或机器链路才展开完整能力基线，先接通新入口和消费者，再退出旧路径。不能先让资源失去消费者，再把它判成无用。

### 3. 先替换，再决定是否增加机制

默认修法是在最早错误位置改成一条能直接驱动正常请求的正面规则，并把同一决定的旧入口、同义提醒、反面清单和冲突校验一起退出。上层已经正确时，直接修下层路由或消费者，不能再给上层加补丁。

只有现有上下文、完整例子和一条自然语言规则仍无法可靠完成结果，而且失败会实质损害用户结果时，才增加分支、状态、schema、脚本、索引或校验器。增加前必须说明它服务哪类结果、由谁消费以及为什么简单机制不足。

### 4. 让每层只收到自己需要的内容

真实前置放在动作前，条件输入按需加载，生产输入只交给当前消费者，依赖成品或运行结果的检查留到对象产生后。治理理由和纠偏过程只影响设计决定，不进入下游提示词；创作参考、研究证据、机器合同和高风险授权各自留在需要它们的阶段。

### 5. 在写入前确认实际变化

用户只要求诊断、解释或审计时保持只读。准备写入时，一次说明未来行为、影响文件和用户可见结果；会改变默认能力、动作权限、材料职责或现有交付时单独说明。用户看过方案后明确要求实施、确认或修复，就直接进入实现，不重复确认。

目标 Skill 已有 Git 仓库和配置好的 GitHub upstream 时，获准创建、修改或迭代 Skill 已经包含把本轮精确改动提交、推送到现有 upstream 并核对远端 HEAD；用户选择只改本地时才停止在本地。创建远端、改变仓库可见性、强制推送、删除、改变未经确认的目标分支或夹带无关改动仍需单独授权。

### 6. 实现唯一流程

- 普通优化直接修改第 2 步找到的唯一所有者，并同步调整所有直接消费者；相同决定不再留在其它活动位置。
- 新建 Skill 使用 `scripts/init_skill.py` 创建最小入口，再用最终行为更新 `SKILL.md` 和 `agents/openai.yaml`。自然语言能解决的能力不新建 reference、script 或 schema。
- 自我进化只保存会改变未来请求的稳定方法，不保存纠正故事；能力迁移先接通目标入口和全部消费者，再退出旧实现。
- 创作参考保持全文可读并交给模型综合；研究证据支撑结论；机器资源才使用标识、版本和生产消费合同；高风险动作只在获准范围内执行。
- 下层发现上层条件不成立时报告并停止，不自行换路线。需要删除文件时先取得许可；未获许可时按仓库规则归档。

### 7. 按结果性质验证

先运行 `scripts/quick_validate.py <skill-folder>` 检查结构，再做直接覆盖本次改变和真实消费者的最低充分验证。只有公共合同确有更多消费者、目标检查暴露系统性问题或用户明确要求时才扩大范围；修改校验器本身时另运行 `scripts/self-test-quick-validate.py`。

- 创作与开放判断审读成品和实际参考上下文；研究核对关键来源与推断；确定性机器产物让同一份真实产物经过生产者、边界和消费者；高风险动作核对权限、精确目标、外部结果和恢复办法。
- 路由或文字修改用代表性请求核对选择与停止位置。混合任务逐阶段验证，不把机器检查套到创作，也不让创作的灵活性降低事实和权限要求。
- 纠偏后确认被点名的方面已经改变，未点名的用户结果仍然可达；上层正确时，还要确认下层和校验器不再恢复旧流程。
- 修改目标 Skill 的完成路线还要分别核对：已有 GitHub upstream 且未选择只改本地时能够到达精确提交、推送和远端 HEAD；没有远端、无法安全分离改动或明确只改本地时停在准确边界。

### 8. 完成交付

先说目标行为现在怎样工作，再列修改文件和实际验证。只有真实消费者已经产生用户可见结果时，才说迁移完成或已经替换。

目标 Skill 已有配置好的 GitHub upstream，且用户没有选择只改本地时，完成阶段默认只暂存本轮获准依赖闭包，提交并推送当前跟踪分支，再重新读取远端 HEAD 核对；无法与其它工作区改动安全分离时停止并报告。创建远端、改变可见性、强制推送、删除和其它未获准外部动作不包含在这个默认阶段。
