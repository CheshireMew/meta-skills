---
name: meta-skills
description: 用于完整创建、改造、审计、迁移和评估 Codex/Agent Skill 的元技能。Use when the user asks to design or scaffold a new skill, generate its SKILL.md and agents/openai.yaml, refactor or audit an existing skill, infer whether its role is too narrow or broad from available conversation history, migrate scattered workflow rules into SKILL.md/references/scripts/assets, separate transient diagnostic evidence from persistent runtime examples and resources, absorb reusable mechanisms without retaining correction history, or validate skill routing, boundaries, resources, metadata, and observable behavior.
---

# Meta-skills

<!-- META_SKILLS_PROTECTED_CORE_START -->
## 核心原则（必看！）

本节是受保护的行为宪章。普通的优化、精简、重构、迁移或措辞调整，不授权删除、降级、移动、合并、改名或重排这些原则。只有用户在当前请求中明确点名要修改核心原则，才允许改动本节，并同步更新 `core-principles.lock.json`。任何其它改动都必须让保护指纹保持不变。

本节同时约束 Meta-skills 的创建、修改与验收过程，以及它所创建或修改的目标 Skill。只约束设计过程的原则由 Meta-skills 在当前任务中执行；会改变目标 Skill 未来行为的原则，必须转化为目标 Skill 的第一判断、默认路径、条件分支、资源职责、输出或停止标准。目标 Skill 保留具体可执行规则，不复制通用原则原文。

1. **有效能力先保全，再谈精简。** 改造、精简、重构、迁移或审计现有 Skill 前，先按用户结果盘点活动行为和运行资源，记录各自的触发条件、职责、生产者、消费者、可观察结果与停止位置；改造后逐项对应为原位保留、迁移保留或用户明确退出。用户本轮没有提到、文件准备合并、名称相近、实现共置或新路由暂时没有读取，都不能证明能力应当退出。只有触发条件、输入职责、输出合同、消费者和停止位置都等价，才能把两项判定为重复；新链路接通并验证前，旧能力不得退出。最终能力应等于原有有效能力加上本轮新增或修改，再减去用户明确退出的部分。
2. **精准高于复杂，但不能以删能力换简洁。** Skill 和提示词只保留会改变路由、判断、动作、产物、验收或停止位置的内容；在第一条的保全结果内，达到同样行为时采用更短、更直接的写法。普通话能说清就不用技术术语；必须保留的术语只出现于需要精度的位置，并在第一次出现时立即用人话说明。废话、保守套话、重复且没有实质后果的负面约束、失败记录和自然语言回归案例留在当前任务；知识、模板、完整示范、schema、脚本和资产只要仍参与正常生产并有明确消费者，就继续作为运行资源保留。用户明确要求创建或修改产物，或者目标任务本身以产物落盘为完成条件时，才写入对应位置；其它用户可见结果直接完整回复。临时产物只在完成当前结果确实需要中间载体或继续编辑时使用，不自动变成长期保存；长期保存服从用户授权和目标项目规则。
3. **正面流程要在最早判断点解决问题。** 先写正常请求应该怎样判断、行动、交付和停止，再补必要边界。从经验或纠偏中得到的有效判断放到问题发生前最早可以控制的位置；检查和恢复只补充正常流程。
4. **行为验收优先，强度与风险匹配。** Skill 是否成功，先看真实请求触发后选择了什么路径、读取了什么、做了什么、停在哪里，以及用户得到什么。结构检查不能代替行为结果；验证范围根据用户要求、结果复杂度和错误代价决定。
5. **用户本意优先。** 用户明确要求的结果、材料来源和允许动作决定默认范围。领域惯例和完整性增强只能帮助完成这个范围，不能擅自扩大目标、权限或交付。
6. **临时治理证据与运行资源分开。** 失败输出、纠偏、旧方案和为本次核对编写的自然语言请求只在当前任务中帮助找到稳定判断。经过独立依据或用户确认、并在正常生产链中由明确消费者持续读取的模板、完整示范、schema、脚本和资产属于运行资源；写清选择条件、输入、作用和停止位置后，把它们保留在活动 Skill。长期默认来自用户目标、稳定事实、明确约束和可迁移机制；运行资源展示这些机制怎样被使用，其中一次任务的人物、对象、数字、结论和专用措辞不升级为默认。长期检查验证结构、契约字段和明确不变量，行为核对使用当前任务中表层不同的正常请求。
7. **默认路径、按需能力和动作权限分开。** 默认路径独立完成用户没有补充说明时最可能需要的结果；能处理什么与默认允许做什么分别判断。信息来源、联网、运行、安装、写入、发送、深度验证和扩展输出都要写清进入条件与停止位置。
8. **路由由上层一次选择，下层只执行。** `SKILL.md` 是唯一的分流位置：先判断用户要得到的结果，再在同一层选择一个条件互斥的主类型或子类型，随后由上层写清该合同所需的输入、资源、动作权限、执行顺序、输出、验收和停止条件。两项能力只要触发条件、必需输入、执行职责、输出合同、消费者或停止位置不同，就保持不同分支，即使主题、名称或文件形式相近也不合并。reference、script 和 asset 不选择、调用或覆盖其它路径，也不反向决定上层类型；共享步骤由 `SKILL.md` 排在已选分支之后。下层发现输入不满足当前合同或缺少权限时，只返回观察并在自己的边界停止；只有用户新增或改变最终结果、权限，或者 `SKILL.md` 在开始前已经排定下一阶段时，上层才能开启下一条路线，不能让下层输出倒过来覆盖已经选定的合同。
9. **交付要让用户看见结果。** 第一句先回答用户问题；最终交付要说明结果是什么、发生了什么、用户能怎样使用以及是否已经完成。内部字段、分类和检查标签只服务执行，不能代替这些具体说明。
<!-- META_SKILLS_PROTECTED_CORE_END -->

## 路由

路由判断只在本节发生。先确定目标与主路径，再按固定顺序叠加会改变信息来源、动作权限、方法强度、输出或停止位置的条件。工作流和下层资源执行已经选定的职责，不再建立另一套路由。

### 1. 先检查目标

- 目标是 Meta-skills 自身时，先运行 `scripts/quick_validate.py <meta-skills-folder>`。受保护核心校验失败时立即停止；只有用户在当前任务中明确授权修改核心原则，才同步更新核心、锁文件和校验器。
- 目标是其它 Skill 时，先读取目标目录的活动 `SKILL.md`、`agents/openai.yaml` 和全部活动运行资源，确认现有行为、消费者与仓库规则。改造、精简、重构或迁移前，先建立临时能力台账；不能先改变路由，再把因此失去消费者的资源判为无用。

### 2. 为每个独立结果选择一条主路径

- 新建 Skill：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`，完成行为合同、最终目录和实施顺序。
- 重写、更新或审计 Skill：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`，还原现有行为并确定一次性迁移范围。
- 维护、评估、分发、默认启用或源码同步：读取 `references/skill-maintenance-and-evaluation.md`；任务同时改变 Skill 行为时，再读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`。

一个请求包含多个独立结果时，分别建立主路径并分别验收；同一个结果不并列保留多个主路径。

### 3. 按顺序叠加附加方法

主路径确定后，依次判断以下方面。条件缺失时继续当前主路径：

1. 正常成功经验、用户认可、纠偏、旧规则或历史对话会改变职能、默认行为或能力时，读取 `references/absorption-and-governance.md`。
2. 压缩包、仓库、文档、示例 Skill 或大量外部材料用于证明专业能力和实现载体时，在吸收规则之外读取 `references/evidence-distillation.md`。
3. 需要创建、分类、迁移或审计模板、完整示范、schema、脚本、资产、索引或其它运行资源时，读取 `references/resource-design.md`；先按正式消费者确定资源身份和分类依据，再设计生产者、真源、检索与验证链路。
4. 需要创建或更新 `agents/openai.yaml` 时，读取 `references/openai-yaml.md`，并以 `scripts/generate_openai_yaml.py` 作为唯一更新入口。
5. 行为合同完成后，使用已经加载的 `references/instruction-hygiene.md` 完成约束清洗；最终交付前读取 `references/quality-gate.md`。

每项附加方法都要在当前任务中写清进入条件、读取内容、改变的动作以及完成后回到主流程的哪一步。同一方面出现冲突条件时，在本节确定优先顺序后只保留一个生效判断；执行中发现目标或权限条件不满足时，只报告并停止当前动作，不能从下层改走另一条路线。用户随后改变目标或权限时，由 `SKILL.md` 开始新的上层路线。

## 工作流

### 1. 先写行为合同，不先写文件计划

根据用户原话，只写会改变实现的内容：

```text
用户要得到什么：
什么请求触发，什么相邻请求不触发：
默认读取什么、允许做什么：
第一步、主流程和按需分支：
默认直接回复什么，何时才写文件：
做到哪里停止：
用户怎样看出结果已经完成：
```

这份判断只在当前任务中使用，不另存计划文件。用户已经说清时不再追问缺口，继续完成只读分析与行为合同；只有缺口会改变 Skill 身份、动作权限、保存位置或主要结果时才问一个关键问题。是否允许写文件不在此处判断，统一由第 5 步决定。

创建或优化 Skill 时，结合当前对话、现有定位和真实使用判断职能是否过宽或过窄。相邻能力只有共同服务同一用户结果、共享主要来源和动作时才合并；来源、权限或结果不同就保持独立。

核心原则只转成目标领域里真正会执行的判断、动作、输出或停止条件，不复制原则名称和对照表。

### 2. 建立改造前基线

修改现有 Skill 时，在当前任务中按用户结果建立临时能力台账：

```text
能力或运行资源：
什么请求触发：
使用什么输入：
负责什么且不负责什么：
生产者与消费者：
当前阶段：临时证据 / 候选资源 / 已注册运行资源 / 固定测试夹具：
所在边界：Skill 源码 / 共享只读资源库 / 可变运行项目 / 缓存与产物：
正式入口与资源标识：
用户能看到什么结果：
在哪里停止：
本轮处置：原位保留 / 迁移保留 / 用户明确退出：
最终位置与验证证据：
```

台账覆盖活动 `SKILL.md`、reference、script、asset、模板、完整示范、schema、索引和实际消费者。一个文件可以包含多项能力；一个能力也可以由多个资源共同完成。按触发条件、输入职责、输出结构、消费者和停止位置识别能力，不按文件名、主题名、格式相似或目录位置合并。用户指出曾经丢失但仍需恢复的能力时，把它作为待恢复项加入台账；读取归档仍服从仓库规则和用户授权。

只有两项在上述五个方面都等价，才能合并为一项重复实现。用户没有再次提到、当前改造目标较窄、某个新流程没有引用、某个文件显得过长或某资源暂时没有新消费者，都不是退出依据。迁移保留项先建立新入口和消费者，再迁移调用点；文件删除或归档按当前仓库规则取得相应授权。修改完成后，台账每一项都必须能在最终活动 Skill 中找到位置和可观察证据。

### 3. 通过价值保留与抽象门槛

用户反馈、旧规则和外部材料先用于定位缺失判断。只有同时满足下面三点的内容才进入活动 Skill：

1. 它会改变未来请求的路由、动作、输出、验收或停止位置。
2. 去掉当前案例的人物、数字和措辞后，仍能处理同类请求。
3. 如果它是模板、脚本或资产，正常流程确实会读取并产生结果。

不满足时留在当前任务。满足但还没有稳定合同、来源确认、资源标识和正式消费者时，只能进入候选区，不能因为文件存在或本次任务成功就成为活动资源。满足全部条件并完成注册、版本、索引和消费入口后，才进入已注册运行资源。把规则放到问题发生前最早会读取的位置；同一行为和同一资源内容各自只保留一个真源。

这一步只决定新增内容是否值得进入，以及重复表达怎样收敛，不得绕过上一节删除、降级或合并既有能力和运行资源。

### 4. 清洗与分级约束

使用 `references/instruction-hygiene.md` 清理规则：先写正常路径，再删除不改变行为的提醒和反向重复；权限与适用范围写成条件；只有具体重大后果无法用正面流程表达时才保留最短禁止。

用户可见内容使用普通话。来源术语和内部字段只帮助执行；普通话能说清就不用技术术语，必须保留的术语第一次出现时立即解释。

### 5. 在写入前让用户确认实际行为

只读分析、审计和答疑完成结论后直接交付，不需要为了交付结论再请求确认。凡本轮将创建、修改、移动、归档或删除任何 Skill 活动文件，先完成只读审计与行为合同，并在首次写入前向用户一次性展示未来默认行为与按需行为、代表性输出骨架、将变动的文件及原因，以及用户能够观察到的完成结果；到此停止并等待用户明确确认。

只有两种情况不再重复等待：用户在当前请求中明确要求跳过方案确认并立即实施；或者用户已经看到本次行为合同、输出骨架和影响文件，随后明确回复批准、确认、执行或修复等同意实施的指令。最初的“创建”“修改”“修复”目标本身、目标描述已经清楚、用户没有反对或只批准继续分析，都不构成写入确认。用户未确认时只交付分析与建议，不写入文件；取得确认后才进入第 6 步。

### 6. 实现唯一流程

仅在第 5 步已经取得写入确认后进入本节；没有确认时不初始化目录、不生成文件，也不修改、移动、归档或删除现有内容。

- 新建 Skill 时，先确定名称、目标父目录、description 和三项必需 UI 文案。用户给出位置时使用该位置；未给出时使用 `$HOME/.agents/skills`。然后运行 `scripts/init_skill.py <skill-name> --path <parent> --description <description> --interface display_name=... --interface short_description=... --interface default_prompt=...`。初始化器只创建 `SKILL.md` 与 `agents/openai.yaml`，不创建空资源目录、示例文件或辅助文档。
- 初始化后立即用已确认的行为合同替换正文占位内容。已有真实内容并存在明确消费者时再创建 `references/`、`scripts/` 或 `assets/`；新增脚本实际运行，新增资产按已加载的资源验证等级检查并准确报告。资源验证使用当前任务真实存在的生产者和消费者。
- 每个独立结果的主路径、子类型选择、信息来源、动作权限、方法强度、输出顺序和停止条件写进 `SKILL.md`。先在同一上层完成互斥选择，再进入已选类型的固定执行顺序；同一方面只保留一个生效判断。
- 职能发生扩大或缩小时，一次性迁移 `description`、第一判断、主流程、reference 路由、默认输出和停止条件，让最终定位成为唯一活动身份。
- 用户要求退出供应商、模型、工具、脚本、框架或旧入口时，先把该载体拆成外部依赖、领域判断、模板或 schema、传输合同、消费者、校验与恢复机制，按行为合同中的逐项处置矩阵完成迁移。文件共置、同一模块或同一调用栈只说明实现位置相邻，不决定这些能力共同退出。每项迁移保留的能力先接通新的生产者、传输边界和消费者并达到当前任务要求的验证等级，再退出目标实现；用户明确要求退出的行为保持退出。
- 运行资源按照已加载的资源设计方法确定身份、分类、生产者、真源、索引、消费者和验证。reference 承载过重的通用方法、边界明确的专业能力和清单；模板、完整示范、schema、脚本、资产与索引分别按消费者的真实用法建立，不因文件格式或目录相同合并职责。所有 reference 由 `SKILL.md` 直接路由，完成后返回主流程；reference 不读取或调用其它 reference，也不覆盖上层已经选定的结果类型。
- 先分开四个根目录：Skill 源码保存规则、脚本、schema 和随 Skill 分发的共享只读资源；可变运行项目保存用户本次配置、输入和输出；缓存只保存可重建内容；临时产物和诊断证据留在任务工作区。禁止的是把可变项目状态反写进活动 Skill 源码，不是禁止 Skill 持有已经确认、注册、版本化且由正式入口读取的共享资产。
- 一次成功任务中的资产先作为候选；只有来源与使用权明确、资源合同完整、具有稳定标识和版本、进入活动索引、由公开入口按标识解析并完成冷启动消费后，才注册为运行资源。资源可被列出和显式选择，不等于成为默认；默认资源必须有独立的用户确认或稳定产品合同。
- 资源里的数值逐项标明来源：协议不变量、算法默认值或资产专属校准。协议不变量由 schema 固定；算法默认值允许调用者显式调整；坐标、帧段、裁剪、阈值等资产专属校准跟随对应资源版本，不能从一个样本升级成全局规则。
- 重复、确定且手写容易错的动作写成 script；自然语言请求和运行历史留在当前任务或项目自身的受控日志。Skill 完成后，运行 `scripts/generate_openai_yaml.py <skill-folder> --interface ...`，确保 UI 名称、简述和默认提示与最终 `SKILL.md` 一致。
- 按约束清洗结果写入规则。正向合同承担默认行为，条件边界承担选择与授权，硬禁止使用最短且范围明确的表述。
- 一次性迁移所有调用点。先证明能力台账中的迁移保留项已经接通新入口和消费者，再退出确认被替代的实现。下层发现目标、权限、输出或停止条件与当前合同不符时，只把观察返回 `SKILL.md` 并停止当前动作；不能自行选择替代路线，也不能覆盖已选合同。只有用户改变目标或权限，或上层原先已经排定下一阶段时，才由 `SKILL.md` 开启新路线。完成迁移后不保留旧入口、同义路由或冲突说明。被替代文件需要删除时先取得用户许可；未获许可则移出活动 Skill 归档。
- 修改 meta-skills 核心时，同步更新 `core-principles.lock.json` 和 `scripts/quick_validate.py`，三处共同形成一个核心版本。

### 7. 验证行为，并核对运行资源的真实消费

1. 运行 `scripts/quick_validate.py <skill-folder>` 检查确定性结构。
2. 逐项核对改造前能力台账：原位保留项仍可达，迁移保留项的新入口、生产者和消费者已经接通，只有用户明确退出项不再活动。不能用“文件还在”代替能力可用，也不能用“新流程通过”证明其它旧能力未丢失。
3. 检查路由方向：`SKILL.md` 完成任务类型和子类型选择；所有 active reference 都由它直接选择，reference 之间没有调用、覆盖或反向分流。触发条件、输出结构或消费者不同的模板保持独立入口。
4. 按本轮改变的公开路由分别验证，不让一个无关分支的可选依赖失败阻止目标路由执行，也不用目标路由通过掩盖其它失败。至少覆盖本轮实际存在的新建、采用已注册资源、迁移旧资源和最终生产等场景；没有进入本轮范围的场景明确标为未运行。
5. 沿本次代表性真实请求检查触发、读取、动作、输出和停止位置；不要用消费端手写假数据代替正在验证的生产结果。
6. 用户结果涉及已注册资源时，从一个全新的 Skill 外部项目调用正式列出或采用入口，只用资源标识解析资源；证明正式入口读取注册表、定位唯一资源真源并把结果交给下一消费者。旧任务目录里的专用脚本、绝对路径和消费端自造数据都不能充当这条证据。
7. 用户结果涉及脚本、模板、文件或其它消费者时，证明真实产物已经生成并被下一步读取。用户明确排除昂贵的最终合成时，可以在完成正式资源解析、合同校验和消费者加载后停止，但必须准确说明没有验证最终渲染结果。
8. 只有高风险、复杂产物、外部写入或用户明确要求时，才增加独立请求和更深验证。
9. 重读最终用户输出，确认第一段直接回答问题，没有内部字段、无用术语和过程说明。

### 8. 完成交付

使用 `references/quality-gate.md` 做最后检查。回复先说结果，再说明关键文件和实际验证；只有用户要求创建或修改产物，或者目标任务本身以产物落盘为完成条件时才写入文件，其它结果直接回复。
