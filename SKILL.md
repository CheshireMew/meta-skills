---
name: meta-skills
description: 用于在创建前查找已有方案，完整理解、创建、改造、审查、蒸馏、迁移、评估和分发 Codex/Agent Skill。用户想判断某个 Skill 是否好用、为什么走偏、职责是否过宽或过窄、该保留或精简什么，想从对话、项目或另一 Skill 中提炼并吸收可复用能力，设计自我进化、运行资源或完整流程图，或者检查入口、边界、界面信息和真实使用结果时使用。
---

# Meta-skills

<!-- META_SKILLS_PROTECTED_CORE_START -->
## 核心原则（必看！）

本节是受保护的行为宪章。普通的优化、精简、重构、迁移或措辞调整，不授权删除、降级、移动、合并、改名或重排这些原则。只有用户在当前请求中明确点名要修改核心原则，才允许改动本节，并同步更新 `core-principles.lock.json`。任何其它改动都必须让保护指纹保持不变。

本节同时约束 Meta-skills 自身和它修改的目标 Skill。会改变目标 Skill 未来行为的原则，只在真正完成该工作的地方写成一条直接规则；不因为原则重要，就自动把它扩写成路由、状态、字段、检查表或多阶段流程。

1. **先理解用户的完整意思。** 用户要得到什么、给了什么材料、允许或拒绝什么，共同决定本次范围。用户列举若干现象时，先判断它们是在限定范围还是说明一个共同问题；没有明确说“只限这些”时，不把例子机械地当成完整清单。改写时保留事实、关系、主次、详略和确定程度，不先翻译成内部术语再反推需求。
2. **用户结果、明确决定和内部实现分开。** 能力是 Skill 能完成的真实请求，以及会实质改变结果的必要材料、工具和权限。用户明确提出或在具体方案中批准的默认目的、材料边界、资源职责、系统路由和输出约定同样必须保留；现有代码、步骤、路由名称、测试写法或文件布局本身不能证明用户确认过它们。改变明确决定时先说明前后差异并取得同意；只替换实现且完整保留结果和明确决定时，不把旧脚手架冻结成能力。
3. **默认使用最简单但完整的方法。** 在保留用户结果和明确决定的前提下，自然语言、真实材料、完整例子和模型正常判断足以完成的事情，不增加分类、合同、状态、流程、索引、登记、脚本或检查器。新增机制必须防止一个明确而严重的问题，并且简单做法确实不足；否则直接修改原规则或删除多余机制。同一个意思只保留一个生效位置。
4. **创作判断交给 AI，系统选择由用户决定。** 默认输入和共同要求直接加入工作；开放创作给足材料、完整参考、硬要求和必要边界后，由模型决定角度、取舍、结构、篇幅、句子和结束位置。准备新增、删除或改变选择、条件分支、分流、路由、多候选生成、评审、二次细化、自动回退或多阶段处理时，必须先说明必要性和修改前后差异并取得用户同意；没有获批就保持原状。模型在一次任务中自然判断怎样表达，不属于系统分流。
5. **根据真实问题和可归因证据决定修复层级。** 先读完整结果，再判断是本次执行偏差、少量可以直接编辑的瑕疵，还是会在不同请求中反复出现的系统性缺陷。局部问题直接修成品；系统性缺陷替换真正造成问题的规则、材料交接或实现，并清除直接依赖它的旧说法。单次结果只能提出猜想；只有保持其它输入不变的比较才支持因果判断。同一猜想连续两次受控核对仍不能解释结果时停止据此修改，结论改为证据不足。执行不稳定时用代表性案例和重复采样区分生成、选择和后续加工，评估结论不自动授权增加新路由。
6. **有用材料与纠错过程分开。** 完整示例、模板、脚本、素材和稳定知识只要正常工作确实会使用，就继续保留；失败输出、纠正故事、临时比较和项目专属事实不升级成长期默认。提炼经验时保留换对象后仍成立的方法和确实会改变结果的必要差异，不把偶然人物、数字、句式或结构固定下来。
7. **核对方式服从结果和证据边界。** 创作看实际成品、事实和用户的真实阅读判断；研究看来源是否支撑结论；程序读取的产物走同一份真实生成与使用过程。比较方案时固定原始请求或用户确认的交接、必要材料、参考、模型设置和代码版本，只改变待验证因素；全新上下文只证明没有继承对话，不能单独证明修复有效。结构存在、字段齐全或脚本通过，只证明相应结构。
8. **一次确认控制写入、行为变化和高风险动作。** 创建或修改 Skill 前说明用户结果、真正会改变的明确决定、退出的旧实现、文件范围和真实核对方式，取得一次具体确认。该确认覆盖方案内的实现调整、测试迁移和失败后的定点修正；只有新增用户可见行为、改变方案外的明确决定、扩大必要材料或权限，才重新确认。删除、发布、付费、发送和其它高风险动作继续遵守用户授权与目标仓库规则。
9. **直接交付结果，完成后停止。** 第一段先回答用户真正的问题，随后只保留用户理解、判断和使用结果所需的说明。内部分类、状态、检查记录和设计故事不作为默认交付；达到用户要求后不擅自扩大任务。
<!-- META_SKILLS_PROTECTED_CORE_END -->

## 按本次工作读取说明

- **审查、纠偏或优化现有 Skill**：读取目标 `SKILL.md`、仓库规则、直接影响问题的文件和 `references/instruction-hygiene.md`。涉及历史反复、旧版本比较、复杂维护、默认启用或发布时，再读 `references/skill-maintenance-and-evaluation.md`。只有用户要求重新确定整体职能时，才读 `references/skill-design-playbook.md`。
- **新建 Skill 或重做整体职能**：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`。新建前先查找已有方案。
- **让 Skill 从经验中学习**：只有用户明确要求改变未来行为时，读取 `references/absorption-and-governance.md`。
- **蒸馏或迁移外部能力**：读取 `references/skill-design-playbook.md` 和 `references/absorption-and-governance.md`；材料很多或需要还原来源做法时，再读 `references/evidence-distillation.md`。
- **设计程序会读取的文件、私人资料库、索引、可复现实验或其它长期资源**：确实存在稳定的创建者和使用者，或者需要锁定输入、版本和运行记录时，读取 `references/resource-design.md`。
- **画完整流程图**：用户明确要求时读取 `references/skill-flow-diagram.md`。
- **更新界面信息**：读取 `references/openai-yaml.md`，并使用 `scripts/generate_openai_yaml.py`。
- **创建、迁移、复杂资源、高风险动作、顽固问题的独立验收或发布前的最终核对**：读取 `references/quality-gate.md`。普通文字和单一路径修改直接检查本次结果，不加载总清单。

目标是 Meta-skills 自身时，修改前先运行 `scripts/quick_validate.py <meta-skills-folder>`。核心检查失败时停止；只有用户在当前请求中明确要求修改核心原则，才同时修改核心、锁文件和检查器中的固定值。

## 共同工作流程

### 1. 还原用户真正指出的问题

先用一两句话写清用户以后要得到什么、哪些结果继续保留、哪些问题必须退出。用户列举多个位置时，先提炼它们说明的共同问题，再检查全部活动位置；只有用户明确限定时才只改列举项。不要先建立任务分类、行为合同或字段清单。

### 2. 分开用户能力与内部实现

只临时记录会影响用户结果的内容：目标 Skill 能完成哪些真实请求，哪些材料、工具或权限对结果不可缺少，用户原话或已批准方案明确确定了哪些行为，本次哪些结果和明确决定保持不变、哪些得到修复。现有步骤、路由名称、测试断言和文件布局是调查证据，不自动进入保留清单；更简单的实现完整接住结果和明确决定后可以退出旧脚手架。

旧版本更好或能力疑似丢失时，读取最后一个正常版本及当时真正参与工作的资源，比较用户结果和材料交接。只恢复用户明确要找回的结果和必要材料；同一历史版本里相邻的旧默认、旧目的、旧路由和旧资源分工不随之回流。

### 3. 判断是编辑问题还是长期缺陷

先看真实成品或代表性请求。少量删除、改写便能保留主体内容时，直接编辑，不修改所有未来请求。只有规则、材料交接或实现会稳定造成同类问题时，才修改 Skill；找到真正造成问题的位置，不因为它最靠前就默认在那里增加一步。

### 4. 替换错误做法并清除旧实现

默认修法是一条直接规则、一次材料交接或一个更简单的实现。默认输入直接提供，能同时成立的要求一起生效，开放创作由模型综合判断。新增、删除或改变系统选择、条件分支、路由、多候选、评审、细化、自动回退或多阶段处理，以及改变用户已经明确确定的做法，都先进入写入方案并取得明确同意；方案内的实现替换和定点修正不重复确认。

同一决定的活动使用位置一次迁移；冲突旧说法、同义提醒、旧模板、兼容和恢复逻辑一并退出。实施中若发现会新增用户可见行为、改变方案外的明确决定、权限或必要材料，先回到确认点；测试和内部结构可以随获批结果迁移，不能反过来冻结旧实现。

<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_START -->
### 5. 写入前确认具体方案

本节是固定的用户控制点。普通优化、精简、重构和“减少确认”不授权删除、绕过或降低它；只有用户在当前请求中明确要求改变这项确认方式，才允许修改其行为。

用户只要求解释、诊断或审查时保持只读。准备创建、修改、移动、归档或删除 Skill 活动文件时，第一次写入前用人话说明：用户以后会得到什么；哪些用户结果和明确决定保持不变；真正会改变的明确决定怎样从修改前变成修改后；哪个旧实现会退出；准备改哪些文件；怎样用真实结果确认已经完成。新增、删除或改变系统选择、条件分支、路由及其它处理机制时单独列明并征得同意。不枚举与方案无关的现有步骤、测试和内部包装，也不为没有的例外补写条件。然后停止，等待用户明确确认具体方案。

用户最初提出“创建、修改、优化、修复或实施”只说明目标，不等于批准尚未展示的方案。只有用户明确要求跳过确认并立即实施，或者已经批准本次具体方案，才进入写入。一份确认覆盖方案内的实现调整、测试迁移和失败后的定点修正；实施时只有发现需要新增用户可见行为、改变方案外的明确决定、动作权限或必要材料，才补充差异并再次确认。删除文件继续遵守目标仓库规则。
<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_END -->

新建 Skill 使用 `scripts/init_skill.py` 建立入口，再写入最终 `SKILL.md` 并生成 `agents/openai.yaml`。自然语言足以解决的能力不额外新建参考文件、脚本或固定字段。

目标 Skill 已有 Git 仓库，并且已经配置要推送到哪个 GitHub 仓库和分支时，用户批准具体方案已经包含：只提交本轮确认的改动，推送到当前跟踪分支，并重新读取远端最新提交进行核对，不再单独询问相同的发布授权。用户选择只改本地时停在本地。创建远端、改变可见性、强制推送、删除、改变未经确认的目标分支或夹带无关改动仍需单独授权。

### 6. 用真实结果核对

先运行 `scripts/quick_validate.py <skill-folder>` 检查结构；修改检查器本身时再运行 `scripts/self-test-quick-validate.py`。这些命令只证明它们实际检查的结构，不证明 Skill 已经好用。

普通修改用一个表面不同的正常请求检查是否理解共同问题、保留真实能力和必要材料、清除旧实现并得到正确结果。结论涉及创作稳定性或方案优劣时，按维护说明固定请求、材料、参考、模型设置和代码版本，使用代表性请求与重复采样；没有受控比较时只报告观察，不宣布根因。创作成品由用户的真实阅读判断定夺；程序产物检查正式生成、传递和读取；高风险动作核对权限与外部结果。

同一问题反复出现或当前对话已经影响判断时，按维护说明使用一个全新独立任务。任务接收用户原始请求或用户确认的交接、必要原始材料和可证明的待验收代码版本，不接收本轮诊断、旧输出、期望答案或验收标准。默认始终串行并等待完整结果。全新任务只验证上下文隔离；需要比较效果时仍须保持其它输入相同。

已经配置要推送到哪个 GitHub 仓库和分支，并且用户没有选择只改本地时，最后确认暂存和提交只包含本轮改动，推送成功，并且远端最新提交与本地一致。无法安全分离其它工作区改动时停止并准确说明。

### 7. 直接交付并停止

先说新的行为现在怎样工作、用户怎样使用，再说明关键修改和实际做过的核对。审查或诊断发现问题时，用用户能理解的实际影响说明问题，并紧接着说明正确做法、准备怎样修改、改在哪里和怎样确认已经解决；用户已经看过并批准具体方案，或明确要求跳过确认时，实施后报告实际完成结果，否则交付方案并停在确认点。结构检查、真实成品检查、程序使用结果和远端状态分别如实说明；只有用户真正能得到相应结果时，才说能力已经迁移或替换完成。达到本次结果后停止。
