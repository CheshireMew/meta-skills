---
name: meta-skills
description: 用于完整学习、创建、改造、审计、迁移和评估 Codex/Agent Skill 及其能力的元技能。Use when the user asks to study another Skill end to end and decide what the target should absorb, design or scaffold a new skill, generate its SKILL.md and agents/openai.yaml, refactor or audit an existing skill, infer whether its role is too narrow or broad from available conversation history, create or improve a self-evolving skill that internalizes validated dialogue lessons into its own behavior, transfer a user-result-producing capability across Skills, files, resources, tools, providers, or architectures, draw a detailed workflow, control, data, resource, or verification chain diagram for a Skill, migrate scattered workflow rules into SKILL.md/references/scripts/assets, separate transient diagnostic evidence from persistent runtime examples and resources, absorb reusable mechanisms without retaining correction history, or validate skill routing, boundaries, resources, metadata, and observable behavior.
---

# Meta-skills

<!-- META_SKILLS_PROTECTED_CORE_START -->
## 核心原则（必看！）

本节是受保护的行为宪章。普通的优化、精简、重构、迁移或措辞调整，不授权删除、降级、移动、合并、改名或重排这些原则。只有用户在当前请求中明确点名要修改核心原则，才允许改动本节，并同步更新 `core-principles.lock.json`。任何其它改动都必须让保护指纹保持不变。

本节同时约束 Meta-skills 的创建、修改与验收过程，以及它所创建或修改的目标 Skill。只约束设计过程的原则由 Meta-skills 在当前任务中执行；会改变目标 Skill 未来行为的原则，必须转化为目标 Skill 的第一判断、默认路径、条件分支、资源职责、输出或停止标准。目标 Skill 保留具体可执行规则，不复制通用原则原文。

1. **先按结果性质和失败代价选择方法。** 不把所有任务都做成同一种流程。创作与判断依靠充分材料、多个好例子和模型综合判断；研究与分析按结论风险核实来源；确定性的机器产物才使用 schema、脚本和逐段链路验证；高风险或外部写入先处理权限和可逆性。混合任务逐阶段判断，不能让其中最严格的一段绑架整条流程。
2. **用户本意和可观察结果优先。** 用户明确要求的结果、材料来源和允许动作决定范围。先看用户最终读到、看到、拿到或能够使用什么，再决定内部结构；领域惯例和完整性增强只能帮助完成这个结果，不能擅自扩大目标、权限或交付。
3. **有效能力按用户结果保全，不按实现细节增殖。** 改造现有 Skill 前，先确认它现在能为用户完成哪些事情以及哪些资源确实参与其中。只有差异会改变用户结果、动作权限、失败风险或必要方法时，才保留独立分支；字段、文件、名称、格式或中间步骤不同，不自动构成不同能力。迁移完成前不提前拆掉仍有用的入口，迁移完成后也不保留旧架构和兼容层。
4. **简单是默认，复杂度必须证明自己。** 新增路由、合同、状态、schema、脚本、索引或校验器前，必须说清它防止哪一种会实质损害结果的失败，以及为什么现有上下文、例子、自然语言规则或模型判断不足以解决。说不清就不增加。精准不是堆约束；尤其在创作任务中，不把审美判断伪装成字段、打分、采用记录或流程证明。
5. **正面流程要在最早判断点解决问题。** 先写正常请求应该怎样理解、行动、交付和停止，再补必要边界。从经验或纠偏中得到的有效判断放到问题发生前最早可以控制的位置；检查和恢复只补充正常流程，不能代替正常流程。
6. **验收服从结果性质和风险。** 创作结果主要看成品、事实是否可靠、是否符合用户反馈和真实阅读感受，不要求证明模仿了哪个案例；研究结果核对来源与推断；确定性机器产物沿真实生产和消费链验证；高风险动作增加独立确认。结构检查只验证结构，不冒充结果质量。
7. **治理证据、创作参考和机器资源分开管理。** 失败输出、纠偏和旧方案只用于提炼稳定判断。创作案例与钩子可以分别存放、共同进入上下文，让模型自由综合，不强迫逐项采用或建立机器式追溯。只有确实由程序解析、跨任务寻址或稳定复用的资源，才需要标识、版本、schema、注册和冷启动验证。
8. **路由只保留真正改变做法的选择。** `SKILL.md` 负责按用户结果选择方法；只有会改变结果、权限、风险或必要手段的差异才建立分支。reference、script 和 asset 执行已经选定的职责，不反向改路由。默认路径应当短而完整，按需能力在条件成立时加入，不能为了覆盖所有可能性把普通任务变成长流程。
9. **交付要让用户直接看见结果。** 第一句先回答用户问题；最终交付说明结果是什么、用户怎样使用、是否完成。内部字段、分类、台账和检查标签只服务必要执行，不能代替成品，也不能把过程负担转交给用户。
<!-- META_SKILLS_PROTECTED_CORE_END -->

## 路由

路由判断只在本节发生。先判断当前阶段产出什么、错了会造成什么，再选择足够的方法。工作流和下层资源执行已经选定的职责，不另建一套路由。

### 1. 先判断结果性质和失败代价

- **创作与开放判断**：文案、文章、命名、风格模仿、案例启发、创意筛选。默认把若干相关完整案例、钩子或其它参考一起交给模型综合；只固定事实、用户硬要求和必要格式，不建立逐项采用记录、唯一参考、机器评分或为了证明过程而存在的校验器。
- **研究与分析**：资料提炼、事实判断、解释和建议。根据结论的新旧程度、争议和风险核实来源，区分事实、推断和观点；不因需要核实就把最后表达也改造成结构化合同。
- **确定性机器产物**：程序会读取的文件、schema、索引、转换结果、固定格式或可重复计算。只有这类阶段需要精确字段、生产者与消费者合同、脚本和真实链路验证。
- **高风险或外部动作**：发布、发送、删除、付费、账户操作和不可逆写入。先确认权限、目标和可逆性，再执行并核对外部结果。

混合任务逐阶段选择方法。例如“研究后写短帖”只在研究阶段核实来源，写作阶段仍按创作方式工作；“写文案后生成机器可读发布包”只对发布包使用 schema。不得用最严格阶段的规则统治其它阶段。

多阶段 Skill、路由修改或执行顺序纠偏先建立临时链路拓扑：每个阶段的前置输入、中间产物、生产者、消费者、影响下游的决定、外部副作用和验证对象。所谓最早判断点，是这些前置已经存在且判断仍能控制后续结果的最早合法位置；不能把尚未产生的证据、候选或成品交给前置门槛裁决。

### 2. 再检查目标

- 目标是 Meta-skills 自身时，先运行 `scripts/quick_validate.py <meta-skills-folder>`。受保护核心校验失败时立即停止；只有用户在当前任务中明确授权修改核心原则，才同步更新核心、锁文件和校验器。
- 目标是其它 Skill 时，先读取目标目录的活动 `SKILL.md`、`agents/openai.yaml` 和全部活动运行资源，确认现有行为、消费者与仓库规则。改造、精简、重构或迁移前，先建立临时能力台账；不能先改变路由，再把因此失去消费者的资源判为无用。

当任务要把一项产生用户结果的能力复制、吸收、迁入、搬迁、替换、拆分、合并、升级、重构到新载体或随旧载体退出时，把它识别为**能力迁移**。能力迁移是贯穿当前主路径的共同方法，不是只服务“复刻另一个 Skill”的专用任务；来源和目标可以是 Skill、文件、运行资源、工具、供应商、协议或架构。普通修改没有来源到目标的能力转移时，不增加迁移台账和验证负担。

### 3. 分开本次结果与学习结果

当用户要求目标 Skill 从对话、项目结果或连续使用中学习时，先判断用户是否同时需要两个独立结果：完成当前项目或领域任务，以及改变目标 Skill 未来处理同类请求的行为。两者都被明确要求时分别建立主路径，先完成并验证当前结果，再把其中经过验证的经验交给目标 Skill 的改造路径；只要求其中一个时在该结果完成后停止。

选择吸收方法或设计资源前，先完成学习落点合同：

```text
学习主体：目标 Skill / 当前项目 / 独立共享资源
证据来源：对话、用户认可、纠偏、项目结果、独立依据或其它明确材料
这段材料包含哪些独立经验：
每项经验会改变哪类未来请求的哪个判断、动作、产物、验收或停止位置：
每项经验的共同机制及其适用请求：
每项经验可能同时成立的特殊维度，以及它们分别改变什么：
共同机制与特殊维度的共同消费顺序：
各自唯一真源和正式消费者：
最终真源与正式消费者：
项目专属事实继续由什么项目真源负责：
用户是否明确要求独立经验库，以及它由谁持续消费：
```

- 用户明确要求 Skill 自我进化、内化经验或把经验吸收到自己身上时，学习主体是目标 Skill。稳定机制进入目标 Skill 自身的活动规则、方法、资源合同、脚本、元数据或确定性检查；项目事实继续留在项目自身的真实来源。
- 用户只要求完成、复盘或治理当前项目，而没有要求改变 Skill 时，学习主体是当前项目；在项目结果交付后停止，不自动修改 Skill。
- 独立共享资源只有在用户把它明确列为结果，并且存在跨任务持续读取它的正式消费者时才成立。没有这个结果时，目标 Skill 自我进化直接落到自身，不另建项目经验区或共享经验库。

设计具有自我进化能力的目标 Skill 时，把这份合同转化到目标 Skill 的顶层路由：普通领域任务独立完成；只有用户明确要求学习、吸收或迭代自身时才进入自我进化路径。材料名称里出现“项目经验”不能替代学习主体和最终消费者判断。

- 新建 Skill：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`，完成行为合同、最终目录和实施顺序。
- 重写、更新或审计 Skill：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`，还原现有行为并确定一次性改造范围；用户指出错误行为或本轮会修改目标 Skill 时，同时读取 `references/skill-maintenance-and-evaluation.md`，把诊断决定和验证后的目标仓库发布纳入完成链。
- 流程与链路图：用户明确要求画出或可视化 Skill 的详细流程、调用、数据、资源或验证链路时，读取 `references/skill-flow-diagram.md`；只读请求画当前状态，方案确认前画拟议状态，实施完成后画最终状态，与创建、改造、审计或迁移组合时在对应主路径还原链路后再生成图。一张图不能同时完整、清楚展示全部实质步骤时，拆成总览图与多张局部详图，并展开总览中的实质聚合节点。
- 能力迁移：读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md` 建立目标基线与能力迁移图；读取 `references/absorption-and-governance.md` 判断迁移价值和处置；存在来源 Skill、仓库、文档或其它实现材料时读取 `references/evidence-distillation.md`，还原来源真实能力、追溯原始上游，并区分直接复用与独立方法学习，由此决定目标 Skill README 的第三方资源与致谢责任；涉及案例库、配置、数据、模板、脚本、schema、索引或资产时再读取 `references/resource-design.md`。所有资源都由本节直接选择，完成后返回同一条迁移主路径。
- 维护、评估、分发、默认启用或源码同步：读取 `references/skill-maintenance-and-evaluation.md`；任务同时改变 Skill 行为时，再读取 `references/skill-design-playbook.md` 和 `references/instruction-hygiene.md`。

一个请求包含多个独立结果时，分别建立主路径并分别验收；同一个结果不并列保留多个主路径。

### 4. 只叠加确实需要的附加方法

主路径确定后，依次判断以下方面。条件缺失时继续当前主路径：

1. 正常成功经验、用户认可、纠偏、旧规则或历史对话会改变职能、默认行为或能力，或者正在创建或改造自我进化型 Skill 时，读取 `references/absorption-and-governance.md`。
2. 压缩包、仓库、文档、示例 Skill 或大量外部材料用于证明专业能力和实现载体时，在吸收规则之外读取 `references/evidence-distillation.md`。
3. 需要创建、分类、迁移或审计运行资源时，读取 `references/resource-design.md`。新建或改造 Skill 时，只要正式消费者依赖本机路径、外部程序、私人库或独立资源根目录，也用该文件建立唯一的本机配置入口。创作案例和钩子只需能被找到、读到全文并一起进入上下文；只有程序解析、跨任务寻址或稳定共享的机器资源，才设计注册、版本、schema、生产者和消费者链路。
4. 需要创建或更新 `agents/openai.yaml` 时，读取 `references/openai-yaml.md`，并以 `scripts/generate_openai_yaml.py` 作为唯一更新入口。
5. 行为合同完成后，使用已经加载的 `references/instruction-hygiene.md` 完成约束清洗；最终交付前读取 `references/quality-gate.md`。

附加方法只有在会改变当前结果时才加载。不要为创作任务自动追加资源注册、执行记录、采用证明或确定性校验；不要为简单问答建立完整工程台账。执行中发现目标或权限条件不满足时，返回上层说明并停止当前动作。

## 工作流

### 1. 先写最小行为合同，不先写文件计划

根据用户原话，只记录会改变实现的内容。创作和普通判断使用基础合同；不要为了显得完整补内部字段：

材料包含反馈、修订或纠偏时，先指出用户点名改变的是哪一个方面，例如长度、载体、受众、技术深度、行动方式、主要成品或补充交付。只有这些方面进入变更合同；现有流程中没有被点名的用户可见结果继续沿用，不能把对一个方面的纠正扩成整套输出的删减。

```text
用户要得到什么：
当前阶段属于创作、研究、机器产物还是高风险动作，失败代价是什么：
什么请求触发，什么相邻请求不触发：
默认读取什么、允许做什么：
正常流程和必要的按需分支：
多阶段或路由修改时，各阶段的前置、产物、消费者和依赖次序：
默认直接回复什么，何时才写文件：
做到哪里停止：
用户怎样看出结果已经完成：
涉及学习时，谁在学习、改变什么未来行为、写入哪个真源、由谁消费：
```

只有当前阶段会产出由程序读取的确定性机器产物时，才追加字段、格式、生产者、传输或存储边界、消费者以及交接验证；只有高风险或外部动作才追加权限、可逆性和外部确认。创作任务不追加案例采用表、候选计数、唯一模仿对象、评分表或执行记录。

能力迁移在基础合同之后追加来源、目标和完成表述：明确哪项用户结果正在移动，来源实际怎样产生它，目标以后怎样产生它，本轮是保留来源、替换来源还是拆分到多个目标，以及当前证据最多允许把迁移说到什么程度。不要因为用户使用“复刻”“照搬”或“像它一样”等说法，就把表面文件一致当成目标；仍以用户要在目标端得到的结果为准。

这份判断只在当前任务中使用，不另存计划文件。用户已经说清时不再追问缺口，继续完成只读分析与行为合同；只有缺口会改变 Skill 身份、动作权限、保存位置或主要结果时才问一个关键问题。是否允许写文件不在此处判断，统一由第 5 步决定。

改造现有 Skill 且材料包含纠偏时，再补一句“本次明确改变什么、哪些现有用户可见结果继续沿用”。这用于限定改动范围，不把纠偏过程保存成长期案例。

用户要求诊断或解释一次 Skill 错误行为时，不能以归因或“我做错了”结束。按已加载的维护规则把结论收口为“需要修复”“无需持久修复”或“目前无法判断”，并交付对应的正确行为、修法或不修改理由、唯一所有者、影响范围、验证方法和当前停止位置。

创建或优化 Skill 时，结合当前对话、现有定位和真实使用判断职能是否过宽或过窄。相邻能力只在结果、权限、风险或必要做法确实不同时分开；仅仅来源、文件、字段或中间步骤不同，不另建分支。

核心原则只转成目标领域里真正会执行的判断、动作、输出或停止条件，不复制原则名称和对照表。

### 2. 按用户结果建立改造前基线

修改现有 Skill 时，在当前任务中简要列出现有用户结果、实际入口、参与资源和本轮处置。只有迁移确定性机器链路或高风险动作时才展开到交接字段：

```text
能力或运行资源：
什么请求触发：
适用请求、活动入口和正式消费者：
用户能看到什么结果：
哪些规则或资源确实参与：
本轮处置：原位保留 / 迁移保留 / 用户明确退出：
```

台账覆盖活动 `SKILL.md`、reference、script、asset、模板、完整示范、schema、索引和实际使用入口，但按用户结果归并，不按文件数量扩张。一个结果可以由多个资源共同完成；创作案例和钩子即使分库存放，也可以在写作时共同进入上下文。用户指出曾经丢失但仍需恢复的能力时，把它作为待恢复结果加入；读取归档仍服从仓库规则和用户授权。

登记输出时，把主要成品、用户可见的补充交付和内部过程分开判断。写作说明、参考路径、引用、验证摘要等，只要是默认交给用户且帮助用户理解、核验或使用结果，就是输出合同的一部分；它们出现在正文之后，不等于内部过程。隐藏推理、候选评分、执行日志和只为完成任务服务的中间记录才属于内部过程。

只有差异会改变用户结果、权限、风险或必要做法时才保留为独立能力。用户没有再次提到、文件显得过长或资源暂时没有新引用，都不是退出依据；反过来，字段、文件、名称或格式不同也不是保留独立架构的理由。迁移保留项先接通新入口，再一次性迁移调用点；文件删除或归档按当前仓库规则取得授权。

同时记录 `SKILL.md` 的行数、字符数和直接路由的 reference 数量，作为本次修改前后的临时证据。创建、修改或自我进化完成时，主文件必须同时不超过 220 行和 14,000 个字符；改造前已经超限时，在保全能力和消费链的前提下于同一次改造中完成收敛，未收敛不交付。

涉及能力迁移时，按已加载的设计规则在同一份临时台账中补全迁移图，覆盖每项用户结果的来源触发和活动链、目标入口与消费者、处置、真实运行前提、当前验证等级和缺口；不能用文件清单代替，也不能在缺少目标真实输入时夸大完成程度。

### 3. 通过价值保留与抽象门槛

用户反馈、旧规则和外部材料先按独立用户结果恢复；含失败、纠偏、保护拦截、绕行或残留时，先完成因果排序，再按 `references/absorption-and-governance.md` 逐项比较有效机制、共同路径和会改变动作的特殊条件。只有同时满足下面三点的内容才进入活动 Skill：

1. 它会改变未来请求的路由、动作、输出、验收或停止位置。
2. 去掉当前案例的人物、数字和措辞后，仍能处理同类请求。
3. 如果它是模板、脚本或资产，正常流程确实会读取并产生结果。

不满足时留在当前任务；满足时放进问题发生前最早会读取的唯一真源。创作案例与钩子只要来源清楚、能被检索并把全文交给写作上下文；只有程序解析、跨任务寻址或稳定共享的机器资源才增加注册、版本、索引和冷启动链路。共同机制、可叠加特殊维度及其消费顺序均由已加载的吸收规则确定。

这一步只决定新增内容是否值得进入，以及重复表达怎样收敛，不得绕过上一节删除、降级或合并既有能力和运行资源。

新增复杂机制还要说明它防止的具体重要失败、简单规则为何不足，以及收益为何高于维护成本；否则不增加合同、状态、脚本、schema、索引或校验器。

### 4. 清洗与分级约束

使用 `references/instruction-hygiene.md` 清理规则：先写正常路径，再冻结真正会进入目标生产提示词的内容。治理材料只改变设计决定，除非正式消费者确实需要，不进入提示词；正常生产直接读取的完整案例、模板、精确事实和机器合同继续按原职责保留。随后删除不改变行为的提醒和反向重复，把权限与范围写成条件；具体重大后果无法由正面流程保护时才保留最短禁止。正面表述若只是把判断改写成字段、计数、状态或证明步骤，同样删除。

用户可见内容使用普通话。来源术语和内部字段只帮助执行；普通话能说清就不用技术术语，必须保留的术语第一次出现时立即解释。

### 5. 在写入前让用户确认实际行为

只读分析、审计和答疑完成结论后直接交付，不需要为了交付结论再请求确认。凡本轮将创建、修改、移动、归档或删除任何 Skill 活动文件，先完成只读审计与行为合同，并在首次写入前向用户一次性展示未来默认行为与按需行为、代表性输出骨架、将变动的文件及原因，以及用户能够观察到的完成结果；目标 Skill 位于现有 Git 远端仓库时，确认材料默认把本轮获准依赖闭包的精确提交、推送与远端核对列入完成链，并给出只改本地选项。到此停止并等待用户明确确认。

只有两种情况不再重复等待：用户在当前请求中明确要求跳过方案确认并立即实施；或者用户已经看到本次行为合同、输出骨架和影响文件，随后明确回复批准、确认、执行或修复等同意实施的指令。最初的“创建”“修改”“修复”目标本身、目标描述已经清楚、用户没有反对或只批准继续分析，都不构成写入确认。用户未确认时只交付分析与建议，不写入文件；取得确认后才进入第 6 步。

确认材料中的每一项默认行为变化都要能对应到用户原话或已经成立的退出依据。若方案准备删除、降级或改为按需提供一个现有用户可见结果，而用户此前没有明确要求这项变化，必须把它及其影响作为单独决定展示并取得明确确认；不能把它藏在“整体优化”里，也不能用对整包方案的一句笼统确认替代这项退出依据。

### 6. 实现唯一流程

仅在第 5 步已经取得写入确认后进入本节；没有确认时不初始化目录、不生成文件，也不修改、移动、归档或删除现有内容。

- 新建 Skill 时，先确定名称、目标父目录、description 和三项必需 UI 文案。用户给出位置时使用该位置；未给出时使用 `$HOME/.agents/skills`。然后运行 `scripts/init_skill.py <skill-name> --path <parent> --description <description> --interface display_name=... --interface short_description=... --interface default_prompt=...`。初始化器只创建 `SKILL.md` 与 `agents/openai.yaml`，不创建空资源目录、示例文件或辅助文档。
- 创建或改造自我进化型 Skill 时，先把学习落点合同写进目标 Skill 的顶层路由，再接入经验提炼方法。自我进化的正式消费者是目标 Skill 未来处理同类请求的活动流程；落盘内容必须能在具体判断、动作、产物、验收或停止位置被读取。先映射现有唯一真源并以替换或抽象方式收敛；没有合适所有者且确属通过价值门槛的新能力时，为它建立最小的新唯一真源和正式消费者；不追加纠正历史、案例专用规则或同义提醒。同一经验同时产生共同机制和特殊维度时，把共同机制写入最早共享路径，把每个会改变动作的特殊维度写入各自条件位置，再由目标 `SKILL.md` 排定共同消费顺序；两层不能互相代替或重复维护同一决定。当前项目的事实、状态和专用结论继续由项目真源负责；独立经验库按用户明确结果和正式消费者另行设计。
- 初始化后立即用已确认的行为合同替换正文占位内容。只有内容确实太长、需要复用或需要确定性执行时才创建 `references/`、`scripts/` 或 `assets/`。能用少量自然语言和完整例子解决的创作方法，不升级成脚本或 schema。
- 每个独立结果的主路径、子类型选择、信息来源、动作权限、方法强度、输出顺序和停止条件写进 `SKILL.md`。互斥选择只在区分证据已经产生后发生；执行顺序由前置、生产者、消费者、副作用和验证对象推导，不套固定阶段顺序；同一方面只保留一个生效判断。
- 职能发生扩大或缩小时，一次性迁移 `description`、第一判断、主流程、reference 路由、默认输出和停止条件，让最终定位成为唯一活动身份。
- 实施能力迁移时，以第 2 步的迁移图为唯一处置范围。复制或吸收允许来源继续活动；替换、重构、拆分、合并、协议升级和载体退出则先建立每项保留能力的目标入口、真实运行前提、生产者、边界和消费者，迁移全部活动调用点并达到当前任务要求的验证等级，再按用户已经确认的处置退出旧实现。文件共置、同一模块或同一调用栈只说明实现位置相邻，不决定能力共同迁移或共同退出；用户明确退出的行为不进入新链路。
- 运行资源按真实用法处理。创作案例、钩子和风格参考保持轻量：来源、全文和足够检索的标签即可；管理上可以分库，使用时可以把多个候选一起交给模型综合，不要求选择唯一案例、逐项声明采用或把审美影响结构化。只有程序会解析、资源要跨任务按标识寻址或资产会独立演进时，才设计生产者、真源、索引、版本和消费者合同。
- Skill 源码保存稳定规则和随 Skill 分发的只读参考；可变运行项目保存用户本次配置、输入和输出；缓存只保存可重建内容；临时产物和诊断证据留在当前任务。不要把一次纠偏记录写成长期运行资源，也不要把普通文本案例当成机器资产治理。
- 需要注册的机器资源必须来源与使用权明确，具有稳定标识和真实消费入口，并完成从公开入口解析到消费者读取的验证。创作参考不走这套注册生命周期；能被搜索并把全文放入上下文就是它的活动消费。
- 资源里的数值逐项标明来源：协议不变量、算法默认值或资产专属校准。协议不变量由 schema 固定；算法默认值允许调用者显式调整；坐标、帧段、裁剪、阈值等资产专属校准跟随对应资源版本，不能从一个样本升级成全局规则。
- 重复、确定且手写容易错的动作才写成 script。检索可以用脚本，写作和审美选择仍交给模型；不要为自然语言成品建立执行记录或采用校验器。Skill 完成后，运行 `scripts/generate_openai_yaml.py <skill-folder> --interface ...`，确保 UI 名称、简述和默认提示与最终 `SKILL.md` 一致。
- 按约束清洗结果写入规则。正向合同承担默认行为，条件边界承担选择与授权，硬禁止使用最短且范围明确的表述。
- 一次性迁移所有调用点。先证明能力台账中的迁移保留项已经接通新入口和消费者，再退出确认被替代的实现。下层发现目标、权限、输出或停止条件与当前合同不符时，只把观察返回 `SKILL.md` 并停止当前动作；不能自行选择替代路线，也不能覆盖已选合同。只有用户改变目标或权限，或上层原先已经排定下一阶段时，才由 `SKILL.md` 开启新路线。完成迁移后不保留旧入口、同义路由或冲突说明。被替代文件需要删除时先取得用户许可；未获许可则移出活动 Skill 归档。
- 修改 meta-skills 核心时，同步更新 `core-principles.lock.json` 和 `scripts/quick_validate.py`，三处共同形成一个核心版本。

### 7. 按结果性质验证

所有修改先运行 `scripts/quick_validate.py <skill-folder>` 检查 Skill 活动资源的确定性结构，再核对改造前列出的用户结果仍然可达；修改校验范围时另运行 `scripts/self-test-quick-validate.py`。其它检查执行前，临时写清验收主张、改动位置、实际消费者、最低充分检查和升级条件；验证范围和证据深度分别决定。先做直接覆盖本次改动和验收主张的目标检查，只有改动的公共合同确有额外消费者、目标检查暴露系统性问题、用户明确要求或错误代价足以支持时才扩大范围；仓库共置、全量入口名称和发布本身不是扩展理由。随后按改动风险选择验证层级，只做与结果性质相称的验证：

- **路由与文字合同**：只改路由、触发、停止位置或文字合同时，用代表性请求和必要的相邻边界核对选择结果、入口与停止位置，不为了验证路由生成完整下游创作成品。
- **创作与开放判断**：创作类修改默认检查指令、参考输入和上下文组合是否正确，不要求“用一条真实请求完整跑一遍”。只有验收明确主张成品效果，且现有成品、案例或更小范围样例不足以判断时，才在当前任务中生成最小必要样例。隔离会话不是默认验证工具；只有用户明确要求，或必须证明结果不依赖当前会话且没有更低强度办法时才启用。
- **研究与分析**：核对关键事实、时效和推断边界；来源强度与结论风险相称。
- **确定性机器产物**：沿真实生产者、传输或存储边界和消费者验证，使用同一份真实产物交接，不用消费端手写假数据冒充核心链路。需要注册的机器资源从公开入口按标识冷启动解析。
- **高风险或外部动作**：核对权限、精确目标、执行结果和恢复办法；必要时增加独立确认。
- **自我进化**：确认普通领域任务不会自动改写 Skill，用户明确要求学习时才把稳定机制写入目标 Skill；项目事实仍留在项目真源。

能力迁移使用 `references/skill-design-playbook.md` 的统一能力与资源验证等级逐项核对迁移图。结构可达只证明目标入口、职责和预期链路已经建立；真实实例化还要证明目标所需的实际配置和真实资源已经就绪；只有正式生产者经过真实边界被目标消费者读取并产生用户可观察结果，才达到端到端消费。临时目录、虚构语料、手写替代数据、固定测试夹具或被 mock 的核心消费者不能把完成等级提升到它们没有证明的位置。

混合任务分别验证各阶段，不把机器产物的检查表套到创作成品上，也不因创作阶段灵活就放松事实或外部动作。最后重读用户可见输出，确认第一段直接回答问题，没有把内部过程当成交付。

路由或多阶段链路发生变化时，再核对每个选择点取得了所需证据、每个消费者只读取已经产生的真实结果、昂贵核实只覆盖会进入当前结果的主张或对象、阶段约束没有污染其它阶段，并让验证发现回到对应生产阶段完成修订。

若本轮由反馈或纠偏触发，再比较改造前后的完整用户可见输出：被点名的方面应当改变，未被点名的主要成品与补充交付应当继续出现；只有用户明确要求退出的结果才应消失。

### 8. 完成交付

使用 `references/quality-gate.md` 做最后检查。回复先说结果，再说明关键文件和实际验证；能力迁移逐项报告实际达到的等级和仍缺边界，只有端到端消费已经证明时才使用“迁移完成”“能力等价”或“已经替换”等表述。创建、改造或迭代目标 Skill 且确认合同未选择只改本地时，按 `references/skill-maintenance-and-evaluation.md` 精确提交并推送目标仓库，核对远端 HEAD 后才完成；远端、认证、分支保护、分叉或未获准依赖阻塞时停在准确边界。其它结果只在用户要求创建或修改产物，或目标任务本身以产物落盘为完成条件时写入文件。
