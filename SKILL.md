---
name: meta-skills
description: 用于判断一项能力应由模型或 Agent、确定性程序、软件或服务还是组合完成，并选择提示词、模板、Skill、CLI、API、GUI 或插件等合适包装与入口；也用于在创建前查找已有方案，完整理解、创建、改造、审查、蒸馏、迁移、评估和分发 Codex/Agent Skill。用户想选择能力载体，判断某个 Skill 是否好用、为什么走偏、职责是否过宽或过窄、该保留或精简什么，想从对话、项目或另一 Skill 中提炼并吸收可复用能力，设计自我进化、运行资源或完整流程图，或者检查入口、边界、界面信息和真实使用结果时使用。
---

# Meta-skills

<!-- META_SKILLS_PROTECTED_CORE_START -->
## 核心原则（必看！）

本节是受保护的行为宪章。普通的优化、精简、重构、迁移或措辞调整，不授权删除、降级、移动、合并、改名或重排这些原则。只有用户在当前请求中明确点名要修改核心原则，才允许改动本节，并同步更新 `core-principles.lock.json`。任何其它改动都必须让保护指纹保持不变。

本节同时约束 Meta-skills 自身和它修改的目标 Skill。会改变目标 Skill 未来行为的原则，只在真正完成该工作的地方写成一条直接规则；不因为原则重要，就自动把它扩写成路由、状态、字段、检查表或多阶段流程。

1. **先理解用户的完整意思。** 用户要得到什么、给了什么材料、允许或拒绝什么，共同决定本次范围。用户列举若干现象时，先判断它们是在限定范围还是说明一个共同问题；没有明确说“只限这些”时，不把例子机械地当成完整清单。改写时保留事实、关系、主次、详略和确定程度，不先翻译成内部术语再反推需求。
2. **用户结果、明确决定和内部实现分开。** 能力是 Skill 能完成的真实请求，以及会实质改变结果的必要材料、工具和权限。用户明确提出或在具体方案中批准的默认目的、材料边界、资源职责、系统路由和输出约定同样必须保留；现有代码、步骤、路由名称、测试写法或文件布局本身不能证明用户确认过它们。改变明确决定时先说明前后差异并取得同意；只替换实现且完整保留结果和明确决定时，不把旧脚手架冻结成能力。
3. **默认使用最简单但完整的方法。** 在保留用户结果和明确决定的前提下，自然语言、真实材料、完整例子和模型正常判断足以完成的事情，不增加分类、合同、状态、流程、索引、登记、脚本或检查器。只有明确而严重的问题、且简单做法确实不足时，才新增机制或限制；否则直接修改原规则或删除多余机制。增强任何已有能力时沿用已经成立的用户结果、明确决定和必要边界；新增限制必须由当前证据显示的具体损失支持，并且只作用于证据覆盖的范围。当前领域的做法经过跨类型检验后才能进入通用层。同一个意思只保留一个生效位置。
4. **创作判断交给 AI，系统选择由用户决定。** 默认输入和共同要求直接加入工作；开放创作给足材料、完整参考、硬要求和必要边界后，由模型决定角度、取舍、结构、篇幅、句子和结束位置。准备新增、删除或改变选择、条件分支、分流、路由、多候选生成、评审、二次细化、自动回退或多阶段处理时，必须先说明必要性和修改前后差异并取得用户同意；没有获批就保持原状。模型在一次任务中自然判断怎样表达，不属于系统分流。
5. **根据真实问题和可归因证据决定修复层级。** 先读完整结果，再判断是本次执行偏差、少量可以直接编辑的瑕疵，还是会在不同请求中反复出现的系统性缺陷。局部问题直接修成品；系统性缺陷替换真正造成问题的规则、材料交接或实现，并清除直接依赖它的旧说法。单次结果只能提出猜想；只有保持其它输入不变的比较才支持因果判断。写入方案必须说明当前证据支持的主要修复依据；实施中若真实结果推翻该依据，或者需要改用新的根因、输入边界、修复层级或验收方法，立即停止目标写入并回到新的方案确认，不能在同一轮换个解释继续修改。
6. **有用材料与纠错过程分开。** 完整示例、模板、脚本、素材和稳定知识只要正常工作确实会使用，就继续保留；失败输出、纠正故事、临时比较和项目专属事实不升级成长期默认。提炼经验时只把重复证据或用户明确确认的稳定方法写成长期默认。准备把做法写进跨类型通用层时，去掉当前对象和领域的专有说法，用结果机制明显不同的 Skill 代入；不能共同成立的内容留在相应领域或目标 Skill，不冒充通用方法。
7. **核对方式服从结果和证据边界。** 先看用户实际得到的结果。能够客观核实的结果，用真实产物、状态和证据判断；允许多种结果成立、体验取决于用户偏好的结果，在用户判断前只中性展示完整结果和可观察差异。测试数量、字段齐全、步骤完成、参考痕迹和其它内部代理指标不能代替最终结果。核对只处理实际结果中已经出现的问题，不把尚未发生的风险预先转换成运行限制。研究看来源是否支撑结论；程序读取的产物走同一份真实生成与使用过程。比较方案时固定原始请求或用户确认的交接、必要材料、参考、模型设置、代码版本和运行方式，只改变待验证因素；全新上下文只证明没有继承对话，不能单独证明修复有效。结构存在、字段齐全或脚本通过，只证明相应结构。
8. **一次确认只覆盖同一方案。** 创建或修改 Skill 前说明主要修复依据、用户结果、真正会改变的明确决定、退出的旧实现、必要输入与材料边界、系统选择、活动资源、文件范围和真实核对方式，取得一次具体确认。确认后可以修正不改变这些内容的实现错误和测试实现；改变主要修复依据、用户可见结果、必要输入、材料边界、路由或阶段、活动资源、文件范围、验收方法、动作权限，都属于新方案，必须先说明差异并重新确认。删除、发布、付费、发送和其它高风险动作继续遵守用户授权与目标仓库规则。
9. **交付真实状态并停止。** 第一段先回答用户真正的问题，随后只保留用户理解、判断和使用结果所需的说明。达到用户结果后停止；获批方案的真实结果验收失败时，也停止目标写入，如实交付失败结果、当前文件状态和需要用户决定的下一步，不在同一轮继续补规则、替换根因、改变测试或扩大范围。内部分类、状态、检查记录和设计故事不作为默认交付。
<!-- META_SKILLS_PROTECTED_CORE_END -->

## 按本次工作读取说明

- **审查、纠偏或优化现有 Skill**：读取目标 `SKILL.md`、仓库规则和直接影响问题的文件。用户原意已经被改写、输入需要跨阶段交接，或者涉及开放创作和参考材料时，再读 `references/instruction-hygiene.md`；涉及历史反复、旧版本比较、复杂维护、默认启用或发布时，再读 `references/skill-maintenance-and-evaluation.md`。只有用户要求重新确定整体职能时，才读 `references/skill-design-playbook.md`。
- **选择能力载体、新建 Skill 或重做整体职能**：读取 `references/skill-design-playbook.md`。需要设计跨阶段输入交接，或者涉及开放创作和参考材料时，再读 `references/instruction-hygiene.md`；涉及确定性程序、Agent 宿主、外部软件或服务、CLI/API/GUI/插件入口、本机配置或长期资源时，再读 `references/resource-design.md`。创建前先按实际执行者、包装和入口查找已有方案。
- **让 Skill 从经验中学习或设计自我维护**：只有用户明确要求改变未来行为时，读取 `references/absorption-and-governance.md`，先区分目标 Skill 职能内的领域资源维护与由 Meta-skills 负责的架构变化；涉及程序读取或长期更新的资源时，再读 `references/resource-design.md`。
- **蒸馏或迁移外部能力**：读取 `references/skill-design-playbook.md` 和 `references/absorption-and-governance.md`；材料很多或需要还原来源做法时，再读 `references/evidence-distillation.md`。
- **设计程序会读取的文件、私人资料库、索引、可复现实验或其它长期资源**：确实存在稳定的创建者和使用者，或者需要锁定输入、版本和运行记录时，读取 `references/resource-design.md`。
- **画完整流程图**：用户明确要求时读取 `references/skill-flow-diagram.md`。
- **更新界面信息**：读取 `references/openai-yaml.md`，并使用 `scripts/generate_openai_yaml.py`。
- **创建、迁移、复杂资源、高风险动作、顽固问题的独立验收或发布前的最终核对**：读取 `references/quality-gate.md`。普通文字和单一路径修改直接检查本次结果，不加载总清单。

目标是 Meta-skills 自身时，修改前先运行 `scripts/quick_validate.py <meta-skills-folder>`。核心检查失败时停止；只有用户在当前请求中明确要求修改核心原则，才同时修改核心、锁文件和检查器中的固定值。

## 共同工作流程

### 1. 只读还原问题与修复依据

完整读取用户指定的会话、项目、结果和活动文件。用人话写清用户以后要得到什么、哪些结果和明确决定继续保留、什么问题必须退出，以及当前证据为什么支持本次修复层级。单次结果的局部瑕疵先直接处理；没有受控证据时只报告猜想，不修改长期 Skill。

工作区是否干净只影响怎样合并、暂存和发布，不决定调查范围，也不决定是否实施。已有修改属于当前真实状态；用户明确要求读取完整会话、完整项目过程或完整历史时，这个完整范围就是输入边界，不能因文件数量或工作区状态自行截断。准备写入时读取准确现状与差异，保留能够辨认的其它工作；只有继续写入会真实覆盖无法辨认的工作时才停止。

创建或修改的目标会写文件时，同时查清所有正式入口、默认参数、下游脚本和工具实际可能写到哪里，并区分 Skill 源码、用户点名的目标项目、Skill 自己的本地运行工作区、外部数据或安装目录、缓存和临时文件。不能只检查主脚本，也不能把当前终端目录当成未声明的默认归属。

### 2. 形成一个可确认的替换方案

方案只说明主要修复依据、未来用户结果、保持不变和真正改变的明确决定、必要输入与材料边界、系统选择、退出的旧实现、活动资源、文件范围和真实核对方式。目标会产生文件时，文件范围还要说明每类文件属于哪个仓库、项目或本机目录，默认路径由什么稳定入口确定，以及已有错放产物是否需要迁移。简单方法能够完整承接时不增加路由、阶段、评审、回退、脚本或登记。方案确认后，一次迁移所有直接使用位置并清除冲突旧实现。

准备把当前经验写入 Meta-skills 的通用原则、共同流程或所有 Skill 都会读取的说明时，先同时分出可能跨类型成立的共同方法，以及会改变领域结果的事实、边界、专业条件和关键资源。只对共同方法中已经确认不改变结果的表面信息做临时代换，再用至少两种结果机制明显不同的 Skill 检查是否仍能自然指导工作。跨类型成立的共同方法进入通用层；必要差异和关键资源留在目标 Skill 或对应的专用说明，并继续进入实际工作。

创建或修改开放创作型 Skill 时，先判断它的正常结果是否允许多种都成立但体验明显不同的表达，并且质量实质依赖用户本人才能提供的情绪、感受、审美、观点、立场或取舍。这个判断只决定目标 Skill 是否需要具备 Plan 模式创作访谈，不成为每次运行时的第二道门槛；一旦具备，用户主动开启 Plan 模式本身就是要求深入访谈的明确信号。

这类目标 Skill 必须在实际运行入口中写清：它不自动开启或关闭 Plan 模式；默认模式继续按正常流程直接完成；Plan 模式开启后不再判断材料是否已经足够，而是先读完现有材料并完成当前权限内需要的调查、联网补充或外部材料发现，再使用 `request_user_input` 每轮追问一个只有用户能够回答的主观问题；用户自然、有辨识度的原话、矛盾和边界进入后续创作材料，不重复询问。访谈不能替代付费、发布、外部写入、分阶段样稿或其它原有授权与确认。具体提问对象按目标 Skill 的创作职责调整，不建立统一问卷、评分或创作字段。

<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_START -->
### 3. 写入前确认具体方案

本节是固定的用户控制点。普通优化、精简、重构和“减少确认”不授权删除、绕过或降低它；只有用户在当前请求中明确要求改变这项确认方式，才允许修改其行为。

用户只要求解释、诊断或审查时保持只读。准备创建、修改、移动、归档或删除 Skill 活动文件时，第一次写入前用人话说明：用户以后会得到什么；哪些用户结果和明确决定保持不变；真正会改变的明确决定怎样从修改前变成修改后；哪个旧实现会退出；准备改哪些文件；怎样用真实结果确认已经完成。新增、删除或改变系统选择、条件分支、路由及其它处理机制时单独列明并征得同意。方案中的每项新增限制必须能够直接追溯到用户明确要求，或者当前证据已经显示的具体损失；没有这种来源就不写入方案，也不因用户确认整套方案而把助手自行补充的限制算作用户决定。用户明确说明某个问题没有发生、不属于本次问题或不需要处理时，该信息只收窄修复范围，不作为保持项、材料边界或新增限制的来源。不枚举与方案无关的现有步骤、测试和内部包装，也不为没有的例外补写条件。然后停止，等待用户明确确认具体方案。

用户最初提出“创建、修改、优化、修复或实施”只说明目标，不等于批准尚未展示的方案。只有用户明确要求跳过确认并立即实施，或者已经批准本次具体方案，才进入写入。确认只覆盖保持主要修复依据、用户结果、必要输入、材料边界、系统选择、活动资源、文件范围和验收方法不变的实现调整与测试实现；任一项改变都停止写入，展示新旧差异并等待新的明确确认。删除文件继续遵守目标仓库规则。
<!-- META_SKILLS_PROTECTED_WRITE_CONFIRMATION_END -->

### 4. 一次实施并清除旧实现

按获批方案修改真正产生问题的位置，一次迁移全部直接使用点。语法、断链、格式和其它不改变方案的确定性实现错误可以直接修正；若需要改变主要修复依据或任何已确认边界，停止写入并回到新方案，不把它称为定点修正。

目标是开放创作型 Skill 时，先单独读取最终会进入创作模型的运行提示词，只保留已经确认的输入、结果和必要边界，再修改测试。测试验证输入、资源、路由和硬边界，不逐字锁定模型应当怎样使用参考、组织内容或形成表达。

目标会写文件时，让所有默认写入路径服从同一份归属决定：优先使用用户明确路径、正式本机配置或由脚本自身位置确定的根目录；只有成品本来就属于调用方项目时，才能以该项目为根。同步修改嵌套脚本、示例和说明，不保留另一个依赖当前目录的隐蔽默认值。

### 5. 核对真实结果并停止

先运行 `scripts/quick_validate.py <skill-folder>`；修改检查器时再运行 `scripts/self-test-quick-validate.py`。它们只证明结构。随后根据结果成立方式执行获批方案中的最低充分真实核对：能够客观核实的结果检查真实产物、状态和证据；依赖用户偏好的开放结果中性展示完整结果和可观察差异，等待用户判断整体体验；研究核对来源，程序让同一份真实产物经过正式生成与读取，外部动作核对准确目标和结果。目标会写文件时，从一个不相关目录调用真实入口，确认产物仍进入已声明归属；再检查调用方和目标仓库的实际文件树、Git 跟踪与忽略状态，不能只检查返回的路径字符串。

真实结果成立时交付并停止。依赖用户偏好的结果在用户判断前保持中性候选状态；能够客观核实的结果按相应证据判断。真实结果失败时也停止：保留准确差异和失败证据，不继续改目标文件，不换根因或测试；说明当前文件是否仍是未验证候选，以及继续工作需要用户确认的新差异。用户选择只改本地时不提交或推送。目标 Skill 已有当前跟踪远端且用户没有选择只改本地时，整个仓库当前工作区是一个不可拆分的发布范围：已跟踪修改、未跟踪文件和现有删除全部一起验证、暂存、提交和推送，不选择性暂存，也不因包含此前本地工作而停止发布；任一部分不能发布时保留整个工作区并停止，不能改成只提交一部分。
