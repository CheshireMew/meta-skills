# 设计或重做一个 Skill

用于新建 Skill、重做整体职能，或者一次改造会影响多个主要能力时。目标是让真实请求稳定得到用户需要的结果，不是把目录和字段填满。

## 1. 先选择最简单但完整的能力载体

用户尚未指定实现方式，或者明确要求比较方案时，先根据最终结果、真实使用方式和运行条件决定怎样完成，不先假定答案一定是 Skill。先分清三个层次：模型或 Agent、确定性程序、具有持续状态的软件或服务等实际执行者；提示词、模板和 Skill 等可复用包装；CLI、API、GUI、插件或 SDK 等使用入口。它们可以组合，但入口名称不能证明背后的能力是否确定、是否具有判断能力或是否保存状态。Codex CLI 是 Agent 的命令行入口，普通工具 CLI 也可能只是确定性程序的入口。

- 只需完成当前一次请求，当前上下文已经足够，也没有长期做法要复用时，直接完成任务。
- 用户要把同一段说明手动交给模型重复使用，模型只需结合每次输入完成工作，不需要自动触发、长期参考、工具协调或资源维护时，交付提示词或模板。
- 模型需要反复识别同类意图、遵守稳定方法、读取长期参考或协调工具，并且用户希望它成为可发现、可安装的长期行为时，使用 Skill。
- 操作重复、确定、需要批量运行或必须得到一致结果，例如解析、转换、排序、生成固定文件和结构检查时，使用确定性程序；根据真实使用方式把它做成脚本，或通过合适的 CLI、API、GUI 等入口调用。
- 结果依赖持续状态、图形交互、账户权限、多人协作、后台运行、实时连接或成熟的专门功能时，优先使用现有软件、服务或 Agent 宿主，并沿用它实际提供的入口。Skill 可以说明或调用它，但不复制外部产品已经负责的状态和功能。

组合方案必须分别说清谁产生主要结果、哪种包装保存可复用方法、用户或系统通过什么入口调用，以及每一部分怎样进入同一份用户结果。例如 Skill 负责理解意图并组织材料，确定性程序负责可重复处理，CLI 只是调用该程序的入口，软件保存长期状态；不能让多个部分重复维护同一规则、数据、状态或入口。最终建议说明用户会得到什么、主要执行者和入口是什么、还需要什么配合，以及更简单做法为什么不能完整满足。

创建任何新载体前，先按实际结果和载体查找已有方案。查找 Skill 时，把用户想得到的结果写成一个自然搜索词，运行：

```text
npx skills find "<query>"
```

第一轮没有实质结果时，最多换一个自然同义词。只阅读真正相关候选的活动 `SKILL.md`，然后说明它能否直接满足、只能提供部分参考，还是仍需新建。涉及外部执行者或 CLI、API、GUI、插件等入口时，检查当前环境已经提供的工具、宿主和用户点名的候选，并查清入口背后的真实能力；确实需要寻找新方案时，再查与结果直接相关的正式来源。安装量、知名度和已经存在只能说明候选可用，不能代替对用户结果、材料、权限、环境和持续成本的判断。

已有方案能完整满足时优先直接使用；只能承担确定性执行、专业判断、状态保存或其它一部分时，把它放进职责清楚的组合；没有合适方案时才创建。用户已经明确选择执行者、可复用包装或使用入口时，不擅自换成另一种；发现它无法完整实现结果时，说明缺口并由用户决定是否改变。

## 2. 从用户原话确定 Skill 要负责什么

先写清：用户真正想拿到什么，提供了什么材料，允许或拒绝哪些动作，哪些只是设计者觉得“更完整”但用户没有要求。

确定长期职能时，优先看用户明确说明的角色和结果，其次看反复出现的相邻请求、重复选择的做法和真实使用结果。一次成功、一次沉默或“业内通常这样做”只能提出猜想，不能直接扩大默认职能。

多项请求如果共同服务一个稳定结果、共享主要材料和专业判断，可以归在同一 Skill。只有用户目标、权限、外部风险或必要专业方法长期不同，才考虑拆成不同 Skill；来源、成品形式或内部步骤不同，本身不要求分流。

## 3. 改现有 Skill 前盘清用户结果

查看主文件及它直接选择的参考说明、脚本、模板、示例、素材和真实使用入口。临时记录：

- 什么请求会使用这项能力或资源；
- 用户最终得到什么；
- 哪些内容和工具确实参与；
- 本次哪些用户结果继续保留，哪些需要修复或退出。

能力以 Skill 能完成的一类真实请求，以及会实质改变结果的必要材料、工具和权限为准。用户原话或已批准方案明确确定的目的、材料边界、资源职责、系统路由和输出约定同样必须保留；当前步骤、路由名称、测试断言和文件布局不能单独证明用户确认过。更简单的实现完整承接结果和明确决定后可以替换；不能用“内部实现”掩盖真实语义变化，也不能用现状冻结偶然脚手架。

用户没有提到某项真实能力，不构成退出依据；当前搜索不到使用位置时先检查是否曾被旧改造切断。保留结果由新做法接通后，被替代的旧实现应当退出，不保留兼容层。

## 4. 写一份短而完整的未来行为说明

用代表性真实请求说明：

```text
用户会怎样提出请求：
最终要得到什么：
需要哪些材料和工具：
默认怎样完成：
用户会看到什么：
```

只有程序必须读取固定产物时，再补字段、格式、生成和读取方式。只有外部写入或高风险动作时，再补权限、准确目标和恢复办法。确实存在不能同时成立的结果或不同机器协议时，直接说明差异；没有时不为了完整补条件。

这份说明离开反面清单后，也应当让另一个执行者知道怎样完成正常请求。没有改变实际做法的字段就是噪声。

目标 Skill 需要使用本机程序、私人资料库、账户或会随机器变化的路径时，只确认这些前提由哪里提供、缺少时哪项能力停止；详细配置留给运行资源方法，不能让本机路径扩张成所有 Skill 的默认设施。

目标会创建、下载、生成、缓存、安装或修改文件时，未来行为说明还要写清这些文件分别属于 Skill 源码、用户点名的目标项目、Skill 自己的本地运行工作区、外部数据或安装目录，还是临时缓存。归属取决于文件由谁继续使用和维护，不取决于调用 Skill 时终端恰好位于哪个目录。路径解析、忽略规则和旧产物迁移也要服从这份归属。

## 5. 让默认工作直接成立

把默认材料、共同要求和正常动作直接写在一起。来源、成品形式、处理深度和表达选择通常可以同时交给模型判断，不需要先变成任务分类或选择节点。

只有用户结果确实不能同时成立、权限不同、存在不可逆外部影响，或者程序必须遵守不同固定协议时，才分别说明做法。深入研究和高成本工具只在结果确实需要时使用，但不把模型可以自然完成的判断写成硬路由。

系统选择、条件分支、路由、多候选、评审、二次细化、自动回退或多阶段处理只有在用户结果确实需要时才进入设计，并作为明确变化写进主方案。模型对一次任务的角度和表达判断不算系统路由。

`SKILL.md` 说明正常工作和何时读取专业资源；参考说明、脚本和素材只提供各自内容，不覆盖用户结果。多个创作参考可以一起进入上下文，不要求主文件预先选定主次、数量或采用方式。

只要 Skill 会写文件，就沿所有入口检查默认输出：顶层命令、嵌套脚本、第三方辅助脚本、下载器、日志、QA、缓存和安装步骤必须服从同一份归属决定。可以接受用户明确传入的其它位置；没有明确路径时，从正式配置或代码自身位置解析稳定根目录。只有用户结果本来就属于调用方项目时，才使用调用方项目根；不能把进程当前目录当作方便的通用默认值。

多阶段工作只需按真实依赖排序：材料和授权先于使用，产生结果的步骤先于读取结果的步骤，必须看到成品才能做的检查放在成品之后，检查失败回到真正产生问题的步骤。不要把一种常见顺序写成所有任务都必须遵守的固定流程。

## 6. 把内容放到真正会使用它的位置

`SKILL.md` 保留触发边界、正常工作、用户权限、必要材料和资源读取说明。过长方法、领域知识和完整示例放到确实会被读取的参考说明或素材中。

自然语言能稳定表达的判断留在说明里。只有重复、确定、手写容易错而且结果必须一致的解析、转换、排序或检查才写成脚本。只有程序必须解析固定字段时才建立固定数据格式。

完整示例、模板、脚本和素材在正常请求确实会读取时可以长期保留。失败样本、纠正故事、临时提示词、临时回答和本次比较记录不进入活动目录。

主文件必须同时不超过 220 行和 14,000 个字符。超出时先合并重复规则、删除内部解释，再把确实必要的详细方法移到参考说明；不能提高上限、拆行规避，也不能为了变短删除用户能力。

## 7. 准备可确认的设计结果

把设计压成一份短而完整的未来行为说明：用户怎样提出请求、最终得到什么、正常工作读取什么、哪些系统选择发生变化、旧做法怎样退出、涉及哪些活动文件，以及什么真实结果能够证明完成。创建、修改和发布授权由主入口统一处理，本说明不另设确认范围。

## 8. 新建 Skill 的顺序

1. 在写入方案中说明 Skill 名称、未来行为和目标父目录。名称使用小写字母、数字和连字符，不超过 64 个字符，目录名与 `name` 一致。用户没有指定位置且当前环境没有另行规定时，使用 `$HOME/.agents/skills`，并用当前系统的实际路径说明；当前环境已经提供其它个人 Skill 根目录时，以那个位置为准。
2. 用户确认方案后，准备列表中显示的名称、25–64 字符简述和一条包含 `$skill-name` 的正常示范请求。
3. 运行 `scripts/init_skill.py` 建立最小入口。目标目录已存在时停止，不覆盖。
4. 用最终行为替换占位内容，只在真实材料就绪时创建参考说明、脚本或素材。
5. 运行 `scripts/generate_openai_yaml.py` 生成界面信息。
6. 运行 `scripts/quick_validate.py`，再重新读取最终 `SKILL.md` 和 `agents/openai.yaml`。

## 9. 迁移能力时判断实际完成到哪里

迁移对象是用户能得到的完整能力，不是旧文件结构。先还原来源怎样使用真实规则、材料、配置和工具产生结果，再确定目标怎样用最简单的完整做法承接；不要求保留来源的路径名、步骤数和固定包装。

完成程度分三层，用人话如实报告：

1. **入口已经接好**：目标写清触发条件和使用位置，所需文件存在，但还没有真实材料或下游任务证明实际使用。
2. **真实材料已经读到**：正式入口能够读取本次真实配置、案例、数据或账户，下一步也收到它，但还没有形成最终用户结果。
3. **用户结果已经产生**：代表性请求从目标入口出发，使用真实材料经过正式步骤，用户最终能看到或使用结果。

不能用临时样本、消费端手写数据或假使用者把前两层说成第三层。缺少下一层所需的真实条件时，停在已经证明的位置并说明缺什么。

## 10. 提交完成证据

设计完成后分别说明入口是否接好、真实材料是否读到、用户结果是否产生；结构存在不能替代后两项。把最终文件、直接引用和实际达到的层级交回主流程核对，不在设计说明中另建一套验收或完成标准。
